
# ? — 6.5/10
VERDICT: Disciplined, on-brief execution that would still lose a beauty contest — because the two service pages I'd project on the same screen for Katie are 80% copy-pasted from each other, and none of the photography could survive a delete-the-logo swap with a competitor's site.

[must-fix] Individual (individual-desktop.png) vs Child & Teen (child-desktop.png) — 'What we work on' block, 'Easy as one, two, three' block, and insurance/self-pay maroon card
  PROBLEM: Put these two pages side by side and the only differences are the hero photo and four bullet lines. The section eyebrow ('WHAT WE WORK ON'), headline ('Care built around your actual week'), the entire maroon insurance/self-pay card (word-for-word including the italic footnotes), and the full 'Easy as one, two, three' module — eyebrow, headline, step copy ('Send your name, email, and phone — or have your provider's office send a referral the same way') — are identical, character for character, across both pages. This is the exact side-by-side I'd open the pitch with: 'here is their $1,500 site's Individual page and Child page — can you tell which is which without the photo?' For a site whose entire premise is 'the same clinician sits across the screen at every visit,' shipping two service pages that are literally the same template with search-and-replace undercuts that promise.
  FIX: Give each service page one structurally distinct proof module instead of the reused dash-bullet list: Individual gets a horizontal pre-op/post-op or 'week 1 / month 1 / month 3' timeline strip; Child & Teen gets an age-band selector (toddler / school-age / teen) as tabbed or accordion content, since the brief calls this her actual differentiator and it currently gets the least bespoke treatment on the site; Corporate gets a simple 3-tier program comparison (lunch-and-learn / ongoing coaching / group contract). Keep the insurance card and 1-2-3 steps as a single shared component (that repetition is fine, even good, for the parts that genuinely are identical across the business) but do not let the 'what we work on' section — the one place page-specific expertise should live — reuse a template with only the bullets swapped.
  EFFORT: days

[must-fix] Home hero (home-desktop.png), Individual hero, Child & Teen hero — all three lead photos
  PROBLEM: Every hero is a stock-feeling 'hands doing a kitchen task' shot — garnishing a plate, chopping something out of frame, kneading dough — with the subject's face cropped out. None of these photos are recognizably Katie, and none signal 'telehealth' or 'clinician,' which is the entire structural claim the copy is making ('the same clinician who verifies your coverage sits across the screen'). Run the delete-the-logo test: swap Tasty Balance's or Jenny Mahoney's logo over these three photos and nothing reads as wrong — anonymous hands with vegetables is the default stock photo for every dietitian site in this category, which the brief itself names as the trap to avoid.
  FIX: Until Katie's real shoot lands, art-direct a single consistent visual device specific to a virtual practice: a laptop or phone visibly in frame with a video-call UI (blurred, not literal enough to be its own cliché), on the same warm kitchen-counter set. That one recurring prop is the cheapest way to make the photography say 'telehealth practice' instead of 'generic wellness brand,' and it's a placeholder note you can hand the photographer as a shot-list item, not a copy rewrite.
  EFFORT: days

[high-value] Site-wide content rhythm — Home, Individual, Child (all three captures)
  PROBLEM: Every content section below the hero uses the identical shape: centered small-caps eyebrow, centered serif headline, centered one-sentence body, then either a 3-column icon row or a 4-item dash list. Home has it four times, Individual has it three times, Child has it three times. There is no asymmetric block, no full-bleed photo insert between hero and footer, no moment where the layout breaks its own rule. Next to a competitor with offset photo/text pairs, pull quotes, or varied column widths, this reads as a single component duplicated down the page — competent, but it hands a competing pitch its opening line for free: 'we'll actually design each section, not stamp one.'
  FIX: Insert one asymmetric editorial module per service page — a photo-left/text-right block breaking the centered rule, sized ~60/40, placed between the 'what we work on' block and the insurance card — to give the page at least one non-centered moment. This is CSS grid, no JS budget spent, and it's the single highest-leverage change to stop the page from reading as templated at a glance.
  EFFORT: days

[high-value] Home, FAQ accordion ('Answers before you ask')
  PROBLEM: Of five FAQ items, only 'Do you accept insurance?' is expanded by default; the other four — including 'Which states can you see clients in?' and 'Are appointments in person or virtual?' — are fully collapsed with no visible answer text. For the stated audience (an office manager who decides in 45 seconds), license-state and virtual-vs-in-person are exactly the two facts that gate a referral decision, and they're hidden behind a click while competing for scan-priority with lower-stakes questions like 'How do I refer a patient?' which currently sits below them.
  FIX: Re-order so 'Which states' and 'Are appointments virtual' sit at positions 1-2 (or expand both by default alongside insurance), and demote 'How do I refer a patient?' Given this is pure content ordering plus possibly one more default-open accordion item, it's a same-day change with zero risk to the shipped delight interaction (rose bar/overshoot) already built.
  EFFORT: hours

[high-value] Home, hero sub-line and top credential strip
  PROBLEM: The hero states credentials/states well ('Registered Dietitian · Licensed in Missouri & New Jersey · Virtual Nutrition Counseling' in the top bar) but the headline itself — 'Nutrition counseling from your own kitchen table' — is a warmth line, not a credibility line, and it's the single largest piece of type on the page. For a referral-source audience scanning in 45 seconds, the top bar credential strip is doing the trust work in ~11px caps while the ~60px headline is doing tone work. Every competitor reference named in the brief (Tasty Balance especially) leads with authority at the dominant type size; here authority is relegated to a thin ribbon above the fold crease.
  FIX: Without inventing new facts, rebalance the visual hierarchy: promote 'Registered Dietitian · Licensed in MO & NJ' (already-approved, already-on-page copy) into a larger line directly above or below the current headline, roughly 2/3 its size — so the eye lands on 'Registered Dietitian, licensed in two states' before it lands on 'kitchen table.' This is a type-scale change, not a content change, and fits inside the Level 2 CSS-only budget.
  EFFORT: hours

[polish] Individual and Child pages, 'Also at Monarch' cross-link section
  PROBLEM: The two pill-style cross-links ('Child & Teen Nutrition →', 'Corporate Nutrition →' on the Individual page, and the mirrored pair on Child) are visually identical outline buttons with no imagery or differentiation — a missed chance to reinforce that these are three distinct practice lines, not three settings of the same button.
  FIX: Give each cross-link pill a small thumbnail crop of that page's hero photo (32-40px, CSS background-image, no added JS weight) so the cross-sell visually previews the destination instead of reading as generic in-page navigation text.
  EFFORT: hours

# ? — 6.5/10
VERDICT: Genuinely disciplined craft that honors every guardrail, but the highest-value trust surfaces for the actual 45-second decision-maker — the insurance line, the reply-time promise, and the About page's own content — are the vaguest, weakest, or literally duplicated things on the site.

[must-fix] About page (src/app/about/page.tsx lines 27-96) — reuses AboutBlock component (src/components/sections/AboutBlock.tsx) verbatim from the homepage
  PROBLEM: The About page literally repeats itself. Its own hero already says 'Katie Sengheiser is a Registered Dietitian licensed in Missouri and New Jersey... clinical focus is weight management and bariatric nutrition... deliberately solo: the clinician who takes the referral, verifies the coverage, and sits across the screen at every visit is the same person' (lines 38-45) — then 46 lines later the page renders <AboutBlock/>, which restates the identical facts almost word-for-word under the headline 'A dietitian your practice can refer to.' A physician's-office manager who lands directly on /about (which she will, from a referral list) reads the same three sentences twice on one scroll. In a genre built on 'clinician talking to a colleague,' repeating yourself reads as either an unfinished build or a padded page — the opposite of the trust it's trying to earn.
  FIX: Stop rendering the shared <AboutBlock/> on the About page. Give About its own distinct module: education/credentialing body (e.g. Commission on Dietetic Registration), years in practice, a philosophy paragraph in Katie's voice, and keep only the license-verification card (the FACTS list + Verify MO/NJ buttons) — not the duplicate headline and paragraph. Reserve AboutBlock's exact current copy for Home only.
  EFFORT: hours

[must-fix] Contact page — ContactForm.tsx line 113: 'Katie replies by phone or email — usually within a few business days.'
  PROBLEM: This is the single sentence a referring office manager reads right after clicking submit, and it directly contradicts the urgency the rest of the site promises ('Katie takes it from there', 'your office gets a clear answer instead of a black hole'). 'A few business days' reads as slow when she has 29 other referral options that likely call back same-day or next-day. For a warm-referral B2B funnel, a vague multi-day SLA is a reason to try the next name on the list, not commit to this one.
  FIX: Tighten to a firm, single-value promise if it's true for Katie's actual practice (e.g. 'Katie replies within one business day' or 'Katie or her office replies the same business day for provider referrals'). If a firm number isn't confirmed yet, at minimum drop 'usually' and 'a few' — vague hedging language is worse than a modest, confident claim.
  EFFORT: hours

[high-value] Home — InsuranceStrip.tsx (rendered directly under the hero, id="insurance")
  PROBLEM: The component's own code comment calls this 'the handoff's highest-value element,' yet the copy that ships is the vaguest sentence on the page: 'Major insurance plans, billed directly — coverage verified before your first visit,' followed by an italic footnote admitting the plan list isn't published yet. This is exactly the information architecture the brief calls the #1 decision point (carrier list up top) and it's currently indistinguishable from every generic 'we accept most insurance' claim the office manager has scrolled past 30 times today. It's not a design failure — it's blocked on Katie's credentialing list — but as shipped it's a 45-second-scan dead zone at the most valuable position on the page.
  FIX: Without inventing carrier names: replace the generic claim with concrete process specificity that IS confirmable today — e.g. 'Send your patient's insurance info — we verify their exact benefits and copay with the carrier within [N] business days, before the first visit is scheduled.' Specificity about the *process* substitutes for specificity about *carriers* until the list lands, and reads far less generic than 'major insurance plans.'
  EFFORT: hours

[high-value] Sitewide header ribbon + Home hero — Hero.tsx lines 30-32 and 60-63, plus the persistent top utility bar ('REGISTERED DIETITIAN · LICENSED IN MISSOURI & NEW JERSEY · VIRTUAL NUTRITION COUNSELING')
  PROBLEM: Before she has scrolled at all, the office manager reads 'Registered Dietitian, Licensed MO + NJ' three separate times: the sticky top ribbon, the hero's small caps line ('Katie Sengheiser, RD · Registered Dietitian'), and the hero's bottom pill badge ('Registered Dietitian Care · Licensed MO + NJ · In-Network Billing'). Three repetitions of the identical fact in one viewport is filler dressed as reinforcement — and it crowds out room for a proof point she doesn't get anywhere on the page: years in practice, credentialing body, or caseload focus that would actually differentiate Monarch from the other 29 names on her list.
  FIX: Cut one of the three redundant credential lines (the hero's top caption is the most disposable, since the pill already restates it more visibly). Use the reclaimed line to surface one differentiating fact once it's confirmed by Katie — e.g. years practicing, CDR registration number, or specific bariatric-program affiliation.
  EFFORT: hours

[high-value] Sitewide CTAs — every primary button reads 'Contact Katie' (Hero.tsx line 49, CtaBand, header nav); referral framing is buried in body copy only
  PROBLEM: The brief's own audience #1 is a referral source, not a patient — yet every CTA on every page speaks patient-first ('Contact Katie'). The referral-specific message ('Send a name and a number — Katie takes it from there') only appears as a paragraph on About and Contact; a scanning office manager who doesn't read that paragraph never learns the same 3-field form is the fast path for her, not just for patients.
  FIX: Add a second, explicitly labeled path near the primary CTA on Home and Contact — e.g. a text link or ghost button reading 'Refer a Patient →' next to 'Contact Katie,' routing to the identical 3-field form (no new fields, no guardrail violation) with an anchor that swaps the intro copy to the referring-provider framing already written on the About page.
  EFFORT: hours

[polish] Home — StatementBands.tsx line 27, orange marquee band: 'Expert nutrition care · In network and online · Wherever your table is'
  PROBLEM: 'Wherever your table is' is the one line on the whole site that leaks into the influencer-dietitian register the brief explicitly bans ('no transform your life filler'). It's mild, but it's placed in a moving, repeating marquee — the highest eye-time real estate on the page after the hero — and it's the only phrase that sounds like it belongs on Jenny Mahoney's site rather than a colleague's referral letter.
  FIX: Swap for a fact-forward marquee using only already-approved claims, e.g. 'Registered Dietitian · Licensed MO + NJ · In-Network Billing · Telehealth Only' — same kinetic marquee treatment, zero new facts, but reads clinical instead of cozy.
  EFFORT: hours

[polish] Home — FAQ accordion (site.ts FAQ array, 5 items)
  PROBLEM: The FAQ set answers what/who/where questions well but skips the question a referring office actually has after 'How do I refer a patient?' — namely 'what happens after I submit, and how fast do I hear back?' Given finding #2 above, this is a second missed chance to state a confident turnaround SLA exactly where a skeptical reader is already primed to look for it.
  FIX: Add a sixth FAQ item, e.g. 'What happens after I send a referral?' answered with the same firm SLA language recommended for the contact page, and mark it inSchema: true so it also strengthens the FAQPage structured data.
  EFFORT: hours

# ? — 6/10
VERDICT: Real scroll-linked CSS craft (view-timeline draws, tile morphs) but the rhythm is metronomic — one reveal move copy-pasted everywhere, the hero's 'parallax' barely moves, and the site's built-but-unused connector line leaves Steps as the flattest beat on the page.

[must-fix] globals.css .reveal (lines 374-388) — used identically by ServiceTiles.tsx, Steps.tsx, AboutBlock.tsx, CtaBand.tsx, InsuranceStrip.tsx, FaqAccordion wrapper on page.tsx
  PROBLEM: Every single section on every page enters with the exact same beat: opacity 0→1 + translateY(24px)→0 over 900ms ease-soft, with only two stagger buckets (0.12s/0.24s). Watching the page scroll top to bottom is watching the same 3-frame move repeat 8+ times back to back — there's no escalation, no section that reads as more important than another. A reel this repetitive would get sent back in dailies: nothing tells the eye 'this next beat matters more.'
  FIX: Build a 3-tier reveal vocabulary via CSS custom properties consumed by one keyframe: --reveal-y (default 24px), --reveal-dur (default 700ms — drop from 900ms, it's sluggish for a 24px move). Micro elements (eyebrow labels, folio numbers) get --reveal-y:8px / --reveal-dur:450ms. Standard cards/paragraphs keep current default. The two 'statement' moments (About intro paragraph, StatementBands copy) get --reveal-y:36px / --reveal-dur:1000ms plus a clip-path inset(100% 0 0 0)→inset(0 0 0 0) mask so they feel like a curtain-lift, not a generic fade-up. Apply via inline style vars per Reveal instance, no new JS.
  EFFORT: hours

[must-fix] src/app/globals.css lines 315-321 (.steps-connector) vs src/components/sections/Steps.tsx
  PROBLEM: The CSS for a scroll-triggered ink line-draw between the three step numbers (animation-timeline: view(), same bfly-ink stroke technique as the butterfly mark) is fully built and even has a reduced-motion + @supports fallback — but Steps.tsx never renders an element with that class. It's dead code. The result: 'Easy as one, two, three' is three static text columns with nothing physically connecting them, on a brand whose entire identity is a hand-drawn ink line. It's the flattest, least-authored section on the homepage sitting right below the two most kinetic ones (hero parallax, tile hover/zoom) — the pacing drops to zero right when it should be building toward the About/FAQ close.
  FIX: Add a thin absolute-positioned SVG polyline (1px, color: var(--color-border-strong) or olive) spanning the three folio numbers at their baseline, class="steps-connector", pathLength="1" so stroke-dasharray:1 math holds regardless of actual path length. Mount it inside the Steps.tsx grid wrapper, position: absolute, top aligned to the '01/02/03' row, hidden below sm breakpoint (stack has no line to draw across). It will animate for free via the existing animation-range: entry 0% contain 50% rule already in globals.css.
  EFFORT: hours

[high-value] src/components/sections/Hero.tsx + globals.css .hero-parallax/@keyframes hero-drift (lines 307-313)
  PROBLEM: The homepage's first and largest motion moment — hero-drift — only moves the image from scale(1) to scale(1.06) plus translateY(14px), linearly across a scroll range of 0–60vh. On an 860px-tall hero that's a nearly imperceptible creep; most visitors scroll past it in under a second of actual movement and never register it as parallax. Worse, it's single-layer: the headline/CTA block sitting on top has zero counter-motion, so there's no depth separation — it reads as 'the photo got slightly bigger,' not parallax.
  FIX: Two changes, both pure CSS animation-timeline (already the pattern in use): (1) widen hero-drift's translateY to ~40px so the drift is legible at normal scroll speed; (2) add a second, independent scroll-linked rule on the text/CTA wrapper (e.g. .hero-copy-drift) with animation-range 0–40vh, translateY(0)→translateY(-16px) — moving opposite/slower than the image creates the actual depth cue a parallax needs. Keep linear timing on both (correct for scroll-linked motion, don't swap to ease-soft here).
  EFFORT: hours

[high-value] src/components/sections/StatementBands.tsx + globals.css .band-lift (lines 344-352)
  PROBLEM: This is the site's one full-bleed color-block 'statement' beat (brown curve → orange ribbon) — structurally it should be the strongest punctuation mark on the homepage, a breath between the tiles and the steps. Instead it carries the thinnest motion budget on the page: band-lift only drifts the copy ±22px (44px total range) via scroll-timeline, which at typical scroll speed is nearly invisible, and the curved white cap (the 'swoosh' into brown) is a static pre-rendered border-radius shape with zero reveal animation. The orange ribbon band beneath has no motion at all — not even on load.
  FIX: (1) Animate the curved cap itself via animation-timeline: view() on the band's entry — scale its height from 0.4 to 1 (transform-origin top, ~500ms feel via view-range entry 0%–30%) so the swoosh visibly happens rather than being pre-set; (2) give the orange ribbon a one-time Reveal-triggered letter-spacing settle (from 0.28em to its resting 0.18em over 500ms ease-soft) so it snaps into focus like a rubber stamp landing, giving the section an actual arrival moment instead of just appearing in the viewport already-formed.
  EFFORT: hours

[high-value] src/components/sections/ServiceTiles.tsx (card hover, line 27) + globals.css .tile-photo (lines 363-370)
  PROBLEM: A single hover gesture on the service tiles fires three unsynced animations: the card's own lift+shadow uses `transition-[transform,box-shadow] duration-300` with NO easing token (falls back to the browser's default linear-ish ease, the only place on the site not using var(--ease-soft)); the photo's scale(1.045) runs 1000ms; the desaturation-removal filter runs 700ms. Mousing over and off a tile quickly shows three different curves resolving at three different times — it reads as three separate effects stapled together, not one authored gesture, which is sloppy next to the discipline shown elsewhere (folio numbers, WONK hover).
  FIX: Standardize all three on var(--ease-soft) and stagger durations deliberately instead of accidentally: card lift+shadow 260ms (fastest, leading), photo scale 500ms (mid), color arriving last at 700ms (color 'warming up' last actually fits the ink/warmth brand story — keep that one, just put it on the same curve). Add `ease-[var(--ease-soft)]` (or a `.tile-lift` utility) to the card's Tailwind transition class so nothing on this interaction defaults to browser-ease.
  EFFORT: hours

[high-value] src/app/page.tsx lines 44-51 — `<Reveal delay={1}><FaqAccordion /></Reveal>`
  PROBLEM: All five FAQ rows are wrapped in a single Reveal, so they fade/rise in as one rigid block simultaneously — the only list on the page (Steps, ServiceTiles both stagger per-item) that doesn't cascade. On the tallest, most text-dense section of the homepage, the one place a gentle staggered reveal would actually read as 'answers arriving one by one' instead gets the least choreography of any list on the site.
  FIX: Wrap each FAQ row in its own Reveal-equivalent with an index-based inline delay (`style={{ transitionDelay: `${i * 70}ms` }}`) rather than the fixed d1/d2 bucket classes (which only cover 3 items) — a 5-item list needs arbitrary per-index delay, not the enum. Cap total stagger under ~350ms so the block still resolves before the user finishes reading the section heading.
  EFFORT: hours

[polish] src/components/sections/FaqAccordion.tsx line 37 — chevron toggle transition
  PROBLEM: The FAQ plus/chevron icon rotates on a bouncy overshoot curve, `cubic-bezier(0.34,1.56,0.64,1)` (a spring with visible overshoot past 100%), while literally every other interactive element on the site — buttons, WONK hover, card lift, reveals — uses the site's signature `--ease-soft` (a clean no-overshoot deceleration). This is the one UI moment on the page with a springy personality, and it clashes with the 'clinician talking to a colleague' quiet-professional brief; it reads like a stray component from a different design system.
  FIX: Swap the chevron rotation to `var(--ease-soft)` at the same 300-350ms duration. If some overshoot personality is wanted for the delight moment (the brief calls out 'FAQ delight (rose bar/overshoot)' as already-shipped), keep it ONLY on the rose accent bar scaleY reveal, not on the icon rotation — one overshoot accent per component, not two competing ones.
  EFFORT: hours

[polish] src/components/ui/MagneticButton.tsx (lines 27-29) + Hero.tsx lines 47-57
  PROBLEM: Magnetic pull is capped at 6px with a 0.12 multiplier on cursor delta — at typical cursor speeds near a 320px-wide button this is close to sub-perceptual, and it's applied only to the primary orange CTA, leaving the adjacent ghost CTA ('Insurance & Coverage') completely static. The asymmetry means the two hero buttons feel like they're from different interaction systems rather than a matched pair, and the one interactive flourish present is too subtle to register as intentional rather than jittery.
  FIX: Either commit to the magnetic effect being felt (raise multiplier to ~0.18-0.2, cap to 9-10px, still restrained) or extend the exact same MagneticButton wrapper to the ghost CTA so both buttons in the pair share one interaction language — asymmetric polish reads as an oversight, not a hierarchy choice, when the two CTAs sit side by side at equal visual weight.
  EFFORT: hours

# ? — 6.5/10
VERDICT: The three-field form itself is well-executed CRO craft, but the funnel leaks at its two highest-intent moments — the homepage's own insurance link routes visitors off-page, and the brief's #1 audience (referral sources) is buried as a footnote on the one page built to convert them.

[must-fix] Home hero secondary CTA — src/components/sections/Hero.tsx line 52-57 (link text 'Insurance & Coverage')
  PROBLEM: The homepage's own InsuranceStrip section has id="insurance" and sits directly below the hero (confirmed in src/app/page.tsx: <Hero/><InsuranceStrip/> back to back). But the hero's 'Insurance & Coverage' button is hardcoded to href="/services/individual-nutrition-counseling/#insurance" — a full page navigation to a different page's insurance/self-pay card, not a same-page anchor scroll. A visitor whose very first click on the whole site is 'tell me about insurance' gets yanked off the homepage into individual-service-specific self-pay copy instead of the general carrier answer that was one scroll away. This is the single highest-leverage leak on the funnel: it actively defeats the brief's '45-second decision' goal by adding a full page load and a context switch at the moment of peak intent.
  FIX: Change the href to "#insurance" (same-page anchor) on the homepage hero so it scrolls to the InsuranceStrip section directly beneath it. If the individual-page destination was intentional for SEO/rate detail, split the two: keep 'Insurance & Coverage' anchoring in-page on Home, and only use the service-page deep link from service-page heroes.
  EFFORT: hours

[must-fix] Contact page — src/app/contact/page.tsx (left column copy) and the italic aside 'Referring providers: the same three fields work for you…'
  PROBLEM: Per the brief, audience #1 is the referring office manager/clinician, yet the Contact page — the entire conversion surface — is written 100% in patient voice ('This form doesn't ask about your health', 'after Katie reaches out'). The referral-source path is a single de-emphasized italic footnote below the fold of the intro copy. An office manager landing here has to read through generic patient reassurance before finding the one sentence that applies to her, and that sentence gives no reassurance about the actual hesitation a referral coordinator has: is it appropriate to submit a patient's name/phone without the patient in the room? That gap adds friction exactly where the highest-value, highest-frequency conversions should be lowest-friction.
  FIX: Add a lightweight two-path toggle above the form — plain radio-style pill tabs ('New client' / 'Referring a patient'), pure CSS+minimal JS content swap, no new fields. Referral tab swaps only the intro paragraph + the reassurance microcopy under the button (e.g. 'Send your patient's name and best contact — Katie handles consent and intake herself once she calls.'). Same 3 fields underneath both tabs, so it stays inside the HIPAA-conscious/no-new-fields guardrail.
  EFFORT: hours

[high-value] src/components/layout/Header.tsx — header wrapper has no sticky/fixed positioning
  PROBLEM: The primary CTA ('Contact Katie') only lives in the top nav and hero. Header.tsx renders as a normal in-flow <header>, not position:sticky, so on the ~5600px-tall homepage the CTA scrolls out of view after the hero and does not reappear until the FAQ block near the very bottom. The long middle stretch — insurance strip, statement bands, service tiles, steps, about block — is a huge span of persuasive content with zero conversion path visible on screen.
  FIX: Make the nav row (not necessarily the top credential bar) `position: sticky; top: 0; z-index: 40` in globals.css / Header.tsx. Pure CSS, no JS, stays well inside the Level 2 (CSS + light JS, no WebGL) budget and adds negligible weight against the 150KB JS cap.
  EFFORT: hours

[high-value] src/app/contact/page.tsx — page composition (no Steps import)
  PROBLEM: The 'Easy as one, two, three' process block (Steps component) appears on Home and every service page but is absent from /contact/ itself. Anyone who lands directly on Contact — from a referral link, Google Business Profile, a colleague's forwarded email, or a QR code on a printed referral pad — never sees the 3-step 'what happens after I submit' framing. At the exact moment a visitor is about to hand over PII, the only forward-looking reassurance on the page is one small sentence under the button ('Katie replies by phone or email — usually within a few business days').
  FIX: Import <Steps/> onto the contact page, placed between the intro copy block and the 'Already working with Katie?' band (or a condensed 3-line vertical variant beside the form) so direct-landing traffic gets the same process reassurance that home/service-page traffic already has before they ever reach the form.
  EFFORT: hours

[high-value] Individual page self-pay card — src/app/services/[slug]/page.tsx lines ~108-124 (the 'Rates publish here the day Katie confirms her fee schedule.' disclaimer)
  PROBLEM: A visitor who scrolls to the self-pay column specifically because they're checking affordability hits a dead end — 'rates publish here' — with zero adjacent recovery action. The nearest CTA is two full sections further down ('Ready when you are'), so the objection (no price yet) is left unresolved right where it surfaces, and the visitor has to keep scrolling and re-motivate themselves to act.
  FIX: Add a small inline text link directly under the self-pay disclaimer, styled as a subtle rose/cream link inside the dark card (not a new field, not a new promise): 'Ask Katie for current self-pay rates →' pointing to /contact/. This converts the exact moment of objection into a low-friction escape hatch instead of asking the visitor to keep hunting for the CTA.
  EFFORT: hours

[high-value] Contact page and post-submit state — src/components/forms/ContactForm.tsx lines 59-62 and src/app/contact/page.tsx form-adjacent copy ('Katie replies by phone or email — usually within a few business days.')
  PROBLEM: This exact sentence is the last thing a referral source reads before deciding whether to submit a patient's contact info, and it appears twice (pre-submit reassurance + post-submit confirmation). 'A few business days' is a soft, non-committal SLA for a referral-driven solo practice — an office manager who needs to close the loop with a waiting patient reads that as risk, and it's the single most consequential line of copy on the highest-friction page of the funnel.
  FIX: Not a design fix — a business-input blocker: get Katie to commit to a firmer, real turnaround (e.g. '1 business day' or 'same business day for referring offices') and swap the copy in ContactForm.tsx (both the idle-state caption and the sent-state paragraph) once confirmed. Do not tighten the language without a real number behind it — that would violate the no-invented-facts guardrail.
  EFFORT: hours

[polish] contact-mobile.png bottom-left corner — visible red 'N · 1 Issue ×' pill (Next.js dev-mode error overlay)
  PROBLEM: The mobile contact screenshot, captured as 'LIVE production build', shows the Next.js dev-tools error indicator badge sitting directly over the footer disclaimer text. If this is genuinely reaching real visitors it is a severe, disqualifying trust-breaker on exactly the page and exactly the audience (a skeptical office manager on her phone) the whole brief is built around — an on-screen 'error' badge next to HIPAA/privacy copy reads as 'this practice's website is broken.' More likely this screenshot was captured against a dev server rather than the production build, in which case the underlying '1 Issue' still needs triaging before it's mistaken for a shipped-clean state.
  FIX: Confirm the screenshot pipeline is hitting the actual `next build`/production output (not `next dev`), and separately open the Next.js dev overlay locally to see what the flagged issue is (likely a hydration mismatch or lint/a11y warning) and resolve it before the next screenshot round.
  EFFORT: hours

# ? — 7/10
VERDICT: Craft and restraint are genuinely strong, but the Child & Teen page — the exact page built to be a standalone ad landing spot for scared parents — skips the one paragraph of human reassurance it needs and ships a confirmed blank-scroll bug on mobile that reads as a broken site at the worst possible moment.

[must-fix] Child & Teen Nutrition page, mobile (child-mobile.png, ~y=3300–5900 of 6400px capture) — confirmed by cropping the screenshot and cross-checking src/app/services/[slug]/page.tsx, where CtaBand is immediately followed by <Footer/> with nothing else in the JSX
  PROBLEM: There is a ~1,800px dead cream gap between the 'Ready when you are' CTA band and the footer on mobile — roughly 4-5 empty phone screens of nothing to scroll through. It does not exist on home-mobile or contact-mobile, and nothing in the component tree accounts for it (CtaBand and Footer are self-contained, no spacer between them in source), which points to a scroll-linked global script (progress-rail / hero-parallax height calc, per the shipped-features list) leaking a height reservation onto a route it wasn't scoped for. For the target persona — a parent already nervous about pressing submit — hitting a seemingly infinite blank scroll right after the CTA reads as 'this site is broken,' which is the worst possible impression right before the contact form.
  FIX: Audit every scroll-linked/ResizeObserver-driven effect registered in the root layout (progress-rail growth, hero parallax, any home-page-only pinned/counter-scroll logic) for whether it sets height/padding on a shared ancestor (body, main, a CSS var) instead of scoping to the component that needs it. Constrain those effects to a page-scoped selector or unmount their listeners on route change. Then add a cheap regression guard: a script that full-page-screenshots all 7 routes at 390px width on every deploy and flags document-height outliers vs. a stored baseline.
  EFFORT: hours

[must-fix] Child & Teen Nutrition page, mobile and desktop — the block from hero straight into 'Care built around your actual week' bullets straight into the brown Insurance/Self-Pay box (child-mobile.png top ~1600px; child-desktop.png upper third)
  PROBLEM: This page goes hero photo → 4 clipped bullet points → billing/insurance financial box, all within one screen and a half on mobile. There isn't a single sentence that validates what the parent is feeling before the site pivots to money and logistics. The bullets themselves are good ('mealtime battles', 'answered plainly') but they're delivered as a bare list, not spoken — there's no line that sounds like Katie actually talking to a worried parent the way the brief's 'clinician talking to a colleague' voice promises for referrers but never extends to the family reading this page directly.
  FIX: Insert one short, plainly-worded paragraph (not a stat, not a testimonial — pure clinical stance, sourced from Katie's draft copy doc, never invented) directly under the hero subhead and before the bullet list — something in the register of 'If mealtimes have turned into a negotiation, you're not doing anything wrong, and neither is your kid.' Style it like the existing italic accent-word treatment (Fraunces italic in the orange-ink color already used for 'your actual week') so it reads as a beat, not a disclaimer. Flag this exact gap to Katie as a copy-doc line item — do not fill it with invented content.
  EFFORT: hours

[high-value] Child & Teen Nutrition page — entire page, both breakpoints (child-desktop.png, child-mobile.png); compare to Home's 'About Katie' block which does not appear on service pages
  PROBLEM: The code comment in src/app/services/[slug]/page.tsx literally says this page is built to 'stand alone (ad-landing discipline)' — meaning a parent may land here cold from a future ad with zero other context about the site. Yet the only image on the page is a faceless stock-style photo of hands rolling dough; there is no photo, headshot placeholder, or trust card for Katie anywhere on this page. The office-manager persona at least gets the credential strip up top; the parent persona gets nothing that puts a human face or personal warmth behind 'family-centered care' before being asked to hand over contact info.
  FIX: Extract the Home page's 'About Katie' card into a shared component and render a condensed version of it on every service page, placed directly after the 'what we work on' bullets and before the insurance box. Even with the headshot still pending from Katie's shoot, use the same placeholder treatment already built for Home rather than omitting the block entirely — it establishes 'this is one specific licensed person, not a directory' before the financial ask, which matters more on the page a scared parent is most likely to land on first.
  EFFORT: day

[high-value] Home page FAQ, 'Do you work with children and teens?' answer (visible collapsed in home-mobile.png; content confirmed in src/data/site.ts lines 117-118)
  PROBLEM: The answer is pure operations: 'Yes — child and teen nutrition is one of Katie's core services, with parents in the room and pediatrician referrals welcome.' This is the exact question a worried parent clicks open hoping for reassurance about tone and approach, and it answers a logistics question instead. It's a missed spot to say anything about how sessions actually feel for a kid, without inventing any fact not already implied elsewhere on the child page (e.g. 'met calmly', 'respects a teen's independence' already exist in the scope bullets — this FAQ just never echoes them).
  FIX: Extend the FAQ answer by one clause pulling directly from language already approved elsewhere on the site, e.g. '...with parents in the room, growth and appetite questions answered plainly, and sessions paced to the child, not a script.' Zero new facts invented — just surface the reassurance that already exists on the Child & Teen page bullets into the place a nervous parent is actually reading it.
  EFFORT: hours

[high-value] Steps component (src/data/site.ts STEPS array, lines 84-96) — rendered identically on Home, Individual, Child & Teen, and Corporate pages
  PROBLEM: 'Reach out / Coverage, verified / Meet Katie' is one hardcoded array with zero per-audience variation. Step 3's body — 'Your first telehealth visit is booked... from your own kitchen table' — is written for an adult self-referral and reads oddly generic on the page about booking a session for your child (whose kitchen table, whose portal login, is it the parent's visit or the kid's?). For a page meant to stand alone as an ad landing spot, reusing the exact same three sentences as the corporate B2B page is a tell that this section wasn't actually written for the parent audience.
  FIX: Give the Steps component an optional per-service override (SERVICES entries gain an optional `steps` array that falls back to the shared STEPS default). Write one child-specific variant for step 3, e.g. 'Your first telehealth visit happens with your kid at the table — booked through Katie's secure portal.' Low-effort, reuses the existing component, no new visual design needed.
  EFFORT: hours

[polish] Contact page, mobile (contact-mobile.png) — the reassurance copy under the heading
  PROBLEM: The copy ('This form doesn't ask about your health... Referring providers: send your patient's name and number') is genuinely good and correctly speaks to the two known audiences (self-referring adult, referring provider) — but never once acknowledges the parent-submitting-for-a-child case, which is the exact moment this persona is asked to commit. It's a small omission but it's the literal last screen before conversion.
  FIX: Add one parenthetical line under the existing copy, matching its italic treatment: 'Referring your child? Same three fields — Katie reaches out to set up the family's first visit.' No new field, no new component, just a copy addition that closes the loop for the one persona this whole review is about.
  EFFORT: hours

# ? — 6/10
VERDICT: Real material craft (grain, ink-draw butterfly, folio steps, drop cap) is sitting on a copy-swapped template skeleton — three service pages and the About page literally repeat each other, mobile ships a broken dead-zone before the footer, and every section on every page is center-symmetric with zero grid breaks.

[must-fix] src/app/services/[slug]/page.tsx — Individual, Child & Teen, and Corporate Nutrition pages (individual-desktop.png, child-desktop.png, corporate-desktop.png)
  PROBLEM: All three service pages are the literal same component with only strings swapped: identical hero band proportions, identical center-caption strip, identical 'what we work on' two-column split, identical brown insurance/self-pay card (same copy on two of three), identical 'Easy as one, two, three' block, identical cross-link pair, identical CTA band. Tab between them and the CMS shows through the copy immediately — this is the textbook 'template wearing nice clothes' tell for a juror who sees 100 sites a week. Worse: the three audiences (a patient choosing a dietitian, a worried parent, an HR/benefits buyer) are as different as audiences get, yet get zero compositional differentiation.
  FIX: Keep the shared data/schema but fork the visual template per audience register. Individual: keep current calm centered read. Child & Teen: break the single hero-band format with an inset two-photo grid under the hero (parent+kid preparing food / a growth-chart-adjacent icon motif) instead of one flat banner — this is the page's stated differentiator and should look like one. Corporate: flip to an asymmetric left-aligned 40/60 'deck slide' layout with small numbered benefit chips instead of the centered patient-facing copy pattern, since a benefits buyer skims a pitch, not a reassurance page. At minimum vary hero aspect ratio, the scope-list layout, and drop the identical brown card from Corporate in favor of a single asymmetric statement. Pure CSS/layout work, no new JS budget needed.
  EFFORT: days

[must-fix] src/app/about/page.tsx (line 92, `<AboutBlock />`) and src/components/sections/AboutBlock.tsx — about-desktop.png
  PROBLEM: The About page restates the same three biographical facts (MO+NJ licensed, weight management/bariatric focus, solo telehealth practice) three times in a row: once in the hero paragraph, again in the 'Send a name and a number' pull-quote band, and a third time verbatim in the reused AboutBlock component — which is the exact same 'A dietitian your practice can refer to' teaser card that already appears on the Home page, license card and all. The one page whose entire job is to give a referring clinician something Home didn't already say instead just echoes Home.
  FIX: Never reuse AboutBlock on /about — it's a homepage teaser, not page content. Replace it with a folio-numbered credentials/training timeline (reuse the already-shipped Steps folio-number motif from Home/services) and a short 'why telehealth' philosophy paragraph in Katie's own draft-copy voice once supplied. Move the license card to a sticky sidebar next to the bio on first scroll instead of repeating it as a third full-width CTA block lower down.
  EFFORT: day

[must-fix] Mobile builds of Individual/Child/Corporate service pages and Contact — child-mobile.png (crop confirms ~1600px of blank cream, roughly two phone-screens, between the CtaBand 'CONTACT KATIE' button and the Footer) and contact-mobile.png (~580px gap). Home-mobile does not exhibit this (FAQ flows directly into the footer)
  PROBLEM: Pixel-cropped the actual PNGs to confirm: on every page that ends in <CtaBand/> (services, likely About too) the mobile viewport renders a large dead cream void before the footer appears — content stops, nothing renders, then the brown footer band finally starts. This reads as a broken page to anyone scrolling on a phone, which is the primary device for a parent checking Child & Teen Nutrition.
  FIX: Bisect on a real mobile viewport: temporarily strip CtaBand's absolute decorative cutout span and the `.progress-rail`'s `animation-timeline: scroll(root)` block (globals.css ~line 288) one at a time to find which is inflating document scrollHeight at narrow widths — scroll-driven CSS animations are a known trigger for scrollHeight miscalculation in Chromium at small viewports. Once isolated, constrain with `contain: layout` on the offending ancestor or move the decorative element out of flow correctly.
  EFFORT: hours

[high-value] Site-wide rhythm — clearest on individual-desktop.png and corporate-desktop.png, but true of home/about/child too
  PROBLEM: Every section on every page is centered, single-column, symmetric text-over-band: hero (centered), insurance strip (centered), statement band (centered), FAQ intro (centered), CTA band (centered), footer (centered). Even the two-column moments (hero split on About/Individual, insurance card) are perfectly even 50/50 grids. Scroll any page and the same label/headline/subhead cadence repeats every ~600px against alternating cream/brown bands — there isn't a single grid break, scale jump, or overlap in the entire build outside the shipped motion details. That predictability is exactly what keeps a site out of Awwwards recognition tier regardless of craft quality elsewhere.
  FIX: Introduce at least two real grid breaks per page using pure CSS transforms (in budget): make the insurance/self-pay card asymmetric 35/65 instead of even 50/50 and let one heading bleed past the card's padding edge; on the Steps section, offset step 2's number vertically (translateY, no JS) so '1, 2, 3' don't sit on a rigid shared baseline. Small moves, but they break the metronome a juror is bored by.
  EFFORT: day

[high-value] Hero.tsx / ServiceHero.tsx across all pages — home-desktop.png, child-desktop.png, corporate-desktop.png
  PROBLEM: Every hero photo is used as flat wallpaper: a full-bleed rectangle, subject dead-centered, headline stacked centered on top of the visually busiest part of the frame. The Home hero literally places the headline over a blurred-out figure with her back to camera and a cluttered counter (lemons, bread, jars) competing for attention directly behind the type. This is the single most common stock-photo-hero template move in the genre — image and copy are laid on top of each other rather than composed together.
  FIX: Treat each hero as an art-directed composition, not a banner: use `object-position` to push the photo's subject to one third of the frame (per page — hands/cutting board for Home, the child's hands for Child & Teen, the presenter for Corporate) and let the headline occupy the resulting negative space rather than sit centered over the subject. No new photography needed, this is crop/positioning + a redesigned text column within the existing Level-2 CSS budget.
  EFFORT: day

[polish] src/app/blog/page.tsx — blog-desktop.png
  PROBLEM: The Blog page is a single small centered 'coming soon' card floating in roughly 40% of empty cream viewport with nothing else on the page. It reads as unfinished rather than intentionally minimal, and it's the page most likely to get an early SEO/social click before Katie has published anything.
  FIX: Even pre-launch, add a 3-up teaser row styled like the Home service tiles listing topic pillars Katie will actually write about (bariatric nutrition, picky eating, insurance FAQs — labels only, no invented stats/testimonials per the no-invented-facts guardrail) so the empty state reads as a preview, not a placeholder.
  EFFORT: hours

[polish] AboutBlock.tsx license card — appears identically on home-desktop.png and about-desktop.png
  PROBLEM: The 'Katie Sengheiser, RD' credential card is a generic bordered SaaS-pricing-card pattern: plain white rounded rect, label/value rows, pill buttons. Nothing about it signals 'clinician's referral note' — it reads as a component pulled from a UI kit, which undercuts the warmer/quieter register the rest of the brand system (grain, ink butterfly, folio numerals) is working to build.
  FIX: Give the card one material distinguishing touch consistent with the register — a thin rose rule under 'Registered Dietitian (RD)' instead of just a color change, or a slight card rotation (1-2deg) with a soft double-shadow to suggest a physical note clipped to the page, echoing the drop-cap/ink-draw craft already shipped elsewhere.
  EFFORT: hours
