
## v2:46431da440316ec6098e427dcc0347bf7aa7ee80bcc7635c904131d78225e4ca  (8 ideas)

### Warm grain overlay — the printed-poster feel  [quick-win · hours]
WHAT: A near-invisible film-grain texture sits over the cream background and brown bands site-wide, so flat color fields read as warm paper instead of flat digital color. Most visible on the curved brown statement band and the hero gradient overlay.
WOW: This single 2KB change is what separates 'nice colors' from 'this feels considered.' It's the exact texture technique 2025-2026 award sites use to keep saturated color from looking sterile/corporate — which is precisely Katie's brief: warmer than a corporate group practice.
FIT: Adds warmth without adding a single new color, icon, or piece of copy — stays inside the approved 5-color palette and reads as quiet craft, not decoration. Zero risk to the clinical/professional read the physician audience needs.
IMPL: Inline SVG feTurbulence (baseFrequency ~0.8, low opacity ~3-5%) encoded as a data-URI background-image, layered with mix-blend-mode: overlay/soft-light on body background and on StatementBands.tsx's brown band. No raster asset, resolution-independent, effectively free against the 150KB JS / 30KB CSS budget since it's pure CSS with an inlined SVG string.
RISK: Must keep opacity low and test on the brown band — turbulence can look like static/dirt if overdone. Verify it doesn't shift LCP (it's a CSS background, not an image request, so should be safe).
URLS: https://css-tricks.com/grainy-gradients/, https://www.freecodecamp.org/news/grainy-css-backgrounds-using-svg-filters/, https://ibelick.com/blog/create-grainy-backgrounds-with-css

### The butterfly draws itself  [quick-win · hours]
WHAT: On first load, the butterfly line-art mark in the header/hero traces itself on screen stroke by stroke over about 1.2s, like a fountain-pen signature — then sits still. On the About page it repeats once, quietly, when the ornament scrolls into view.
WOW: It turns the brand mark from a static logo into a signature moment — the kind of small bespoke touch that makes visitors feel the site was made specifically for this practice, not templated. It's restrained (one draw, not a loop) so it reads as craft, not gimmick.
FIT: It's motion applied to the one asset that's already 100% Katie's own brand mark — no new visual language introduced. Runs once and stops, so it never competes with content or feels 'influencer.' Fully covered by existing motion-safe / reduced-motion handling already in the codebase.
IMPL: ButterflyMark.tsx: set stroke-dasharray to each path's getTotalLength(), animate stroke-dashoffset from length→0 via CSS @keyframes (or Web Animations API) on mount; reuse the existing Reveal IntersectionObserver hook to trigger the About-page repeat on scroll-into-view, fire-once. Wrap in prefers-reduced-motion: no-preference the same way current reveals are gated.
RISK: SVG path lengths must be measured client-side (getTotalLength) — do this in a useEffect after mount to avoid layout shift; keep it inert (no interaction) so it can't be mistaken for a button.
URLS: https://css-tricks.com/svg-line-animation-works/, https://www.cassie.codes/posts/creating-my-logo-animation/, https://css-tricks.com/scroll-drawing/

### Editorial reading details: drop caps and custom selection  [quick-win · hours]
WHAT: Lead paragraphs on the About page and blog posts open with a large italic Fraunces drop cap. Selecting any text on the site highlights it in the rose accent color instead of the browser's default blue.
WOW: These are the tiny typographic tells of a genuinely edited, magazine-quality site — the kind of detail a physician-office reader won't consciously notice but that raises the whole page's perceived craft in under a second.
FIT: Pure typography using fonts already licensed and loaded (Fraunces, already self-hosted). Rose is already an approved accent color, applied here in its correct role (accent, never body text). Zero new dependencies, zero copy risk.
IMPL: ::first-letter{font-family:var(--font-display); font-weight:440; font-style:italic; float:left; font-size:3.2em; line-height:0.8; color:var(--color-orange-ink); padding-right:.08em} on the first <p> of AboutBlock.tsx and blog post bodies; ::selection{background:var(--color-rose); color:var(--color-brown)} globally in globals.css.
RISK: Drop caps need a widow/orphan check on short paragraphs — restrict to paragraphs over ~40 words so it never triggers on a 1-line intro.
URLS: https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/font-variation-settings

### Scroll-driven Fraunces headline weight  [medium · hours]
WHAT: As the hero headline enters the viewport, its Fraunces variable font subtly gains optical size and weight — starting slightly lighter/tighter and settling into its full display weight exactly as it centers on screen, tied to scroll position rather than time.
WOW: This is a genuinely current (2025-2026) technique — CSS scroll-driven typography — that almost no small-practice site has, because it requires a variable font (already chosen: Fraunces) plus modern CSS, not a plugin. It makes the one line of type Katie most wants noticed feel alive without any JS animation library.
FIT: Purely typographic motion on the existing display face — no new color, no new element, nothing that could read as flashy. Falls back invisibly (headline just renders normal) on browsers without support, so it never breaks the clinical, quiet tone.
IMPL: @supports (animation-timeline: view()) block only: @keyframes hero-weight { from { font-variation-settings: 'opsz' 40, 'SOFT' 80, 'wght' 380 } to { font-variation-settings: 'opsz' 144, 'SOFT' 80, 'wght' 440 } } applied to Hero.tsx's h1 with animation-timeline: view(); animation-range: entry 0% cover 40%. Wrapped in prefers-reduced-motion query per existing motion-safe convention.
RISK: Safari 18+/Chrome 115+ only (per MDN/Chrome docs) — must be pure progressive enhancement via @supports, never the only way the headline becomes legible. Test that 'SOFT' axis stays pinned at 80 (approved) throughout.
URLS: https://www.carmenansio.com/articles/variable-font-scroll, https://developer.mozilla.org/en-US/docs/Web/CSS/Guides/Scroll-driven_animations, https://developer.chrome.com/docs/css-ui/scroll-driven-animations

### The referral one-pager (print-ready)  [medium · day]
WHAT: A dedicated, single-screen page — reachable from the header and About page as 'For Referring Providers' — that's designed to be printed or PDF'd on the spot: Katie's headshot, both state license numbers, in-network carrier list, phone/fax, and 'how to refer a patient' in three lines. Hitting Cmd/Ctrl-P produces a clean, letter-size, black-on-cream one-page handout with no nav, no footer, no CTAs — just the facts an office manager pins to a corkboard or faxes.
WOW: This is the single highest-leverage idea for Audience #1. Referral-driven growth lives or dies on what an office manager does in the 3 minutes after finding the site — right now every dietitian site just says 'contact us.' A page built explicitly to survive being printed and handed around a break room is a genuine trust device, and virtually no competitor site does this.
FIT: Zero PHI, zero new form fields, zero scheduling — it's static credentialing information Katie already has to publish anyway (license numbers, carriers), just packaged for the medium referral offices actually use (print/fax/PDF). No invented facts: every field pulls from the same open asset slots already tracked in the build spec (carrier list, license numbers, phone).
IMPL: New route /refer/ (or /for-providers/) reusing ServiceHero.tsx patterns; a print.css module scoped with @media print + @page { size: letter; margin: 0.6in } that sets print-color-adjust: exact (to keep brand brown/orange in the printout), hides Header/Footer/MobileNav via display:none, and forces a single-page grid. Include a small inline SVG QR code (statically generated at build time, no third-party script) linking back to /contact.
RISK: Needs Katie's actual license numbers, carrier list, and fax/phone before it can ship for real — until then it must stay a structural placeholder with clearly marked TODO slots, per the compliance gate on no invented facts.
URLS: https://developer.mozilla.org/en-US/docs/Web/CSS/Guides/Media_queries/Printing, https://alistapart.com/article/goingtoprint/

### Zero-data coverage disclosures (expand the insurance strip)  [medium · hours]
WHAT: Each carrier name/logo in the insurance strip becomes a small disclosure (a plain summary/details reveal, or gently animated accordion) that opens to one or two lines Katie has pre-written: 'In-network — most plans need no referral' or 'Call our office to confirm your specific plan.' Nothing is typed, submitted, or stored anywhere; it's just pre-authored plain-language context sitting behind a click.
WOW: It answers the #1 silent question every visitor has ('will my insurance actually work') without a single input field, quiz, or lead-capture pattern — which is exactly the 'coverage-check UX that collects zero data' the brief asks for, and it upgrades the current insurance strip from a static claim into something visitors can actually interrogate.
FIT: Uses the existing InsuranceStrip.tsx component and existing FAQ accordion pattern (details/summary, rotating plus icon) already built for the FAQ — same interaction language, so it feels native to the site rather than bolted on. No PHI, no chat, no scheduling; strictly reveals static text Katie authors in advance.
IMPL: Extend InsuranceStrip.tsx: map carrier objects → <details><summary>{name}</summary><p>{plainLanguageNote}</p></details>, reusing FaqAccordion.tsx's existing ⊕ rotate-45° CSS. Content is fully static at build time (no client fetch, no state persisted beyond the open/closed DOM attribute) — literally zero network calls, satisfying the CSP/no-pixel constraint by construction.
RISK: Entirely gated on Katie's actual credentialing/carrier list landing (same open asset slot already tracked in build-spec.md) — ships as a structural pattern with the current placeholder copy until then.

### Visit walkthrough: a scroll-scrubbed 4-stage journey  [ambitious · day]
WHAT: The current numbered Steps section becomes a sticky, cinematic sequence: as the visitor scrolls, a slim progress line draws itself stroke-by-stroke while four stages — Referral & Intake, Coverage Verification, First Session, Ongoing Care — crossfade in with a short line of what actually happens at each stage. It scrubs precisely with scroll position, not a timer, so an office manager or nervous parent can scroll at their own pace and re-scroll to re-read.
WOW: This is the flagship 'wow' for Audience #2 (patients/parents) and it doubles as the clearest, fastest 'what to expect' explainer for Audience #1 — turning an abstract step list into something that feels guided and calm, the exact effect scrollytelling has been shown to have on patient anxiety and comprehension in medical-visualization research.
FIT: Built entirely from CSS scroll-driven animation on the current Steps.tsx content — no new copy tone, no WebGL, no animation library, no scheduling or data collection. Degrades gracefully to today's static numbered list on browsers without scroll-timeline support, so it never becomes the only way to read the steps.
IMPL: Wrap Steps.tsx content in a tall sticky container; drive a stroke-dashoffset progress path and stage opacity/translateY via animation-timeline: scroll(nearest) with @supports (animation-timeline: scroll()) as the gate — inside that block only, JS is unnecessary. Outside @supports, render the current static Steps markup unchanged. Keep to CSS per Level 2 budget (no WebGL/R3F, per constraints).
RISK: Firefox scroll-timeline support still trails Chrome/Safari per MDN — must verify the @supports fallback path actually renders the existing static Steps cleanly, and keep total added CSS well under the 30KB budget.
URLS: https://developer.mozilla.org/en-US/docs/Web/CSS/Guides/Scroll-driven_animations/Timelines, https://www.joshwcomeau.com/animation/scroll-driven-animations/, https://flourish.studio/blog/scrollytelling-examples/

### Verify-the-license trust pills  [medium · hours]
WHAT: A small, understated component — 'Licensed in Missouri' and 'Licensed in New Jersey' as two pill badges — that link out directly to each state's official public license-verification lookup, so an office manager can confirm Katie's credentials on the state board's own site in one click, without ever leaving a trail of PHI or filling out anything.
WOW: Nearly every dietitian site just states 'licensed in X' as a claim to be trusted at face value. Linking straight to the state board's own verification search is a pattern borrowed from legal and financial-advisor sites (where 'verify my license' badges are standard) and is essentially unheard of among solo dietitian practices — for a physician's office manager doing due diligence in 45 seconds, this single link can be the deciding trust signal.
FIT: Zero data collection (it's an outbound link to a government site), no new visual system (renders as a pill using the existing button/pill CSS already defined for CTAs), and it's the most literal, understated form of 'clinician talking to a colleague' — here are my credentials, go check them yourself.
IMPL: Small TrustBadges component reusing the existing .btn pill/border styling at a smaller scale, placed in the About page's fact card (kcard) next to the license numbers and optionally in the footer. Each pill is a plain <a> to the Missouri Division of Professional Registration license lookup and the New Jersey Division of Consumer Affairs verification search, target=_blank rel=noopener, with Katie's actual license numbers once issued.
RISK: Needs Katie's confirmed license numbers before the links can be made specific/deep (until then, link to the general state lookup page, not a pre-filled search) — do not fabricate a license number or a direct deep-link result.
URLS: https://envzone.com/license-verification/

## v2:80ff7f6c08d0a1a72d39883e78daf3cbcf29d650eb9b87778646238b84a69afd  (8 ideas)

### Drop cap on long-form bio copy  [quick-win · hours]
WHAT: The opening paragraph of Katie's bio on /about (and optionally the homepage About block's first paragraph) starts with an oversized, italic Fraunces initial letter in orange-ink that sinks two lines into the body copy — a quiet, literary signal that this is a real clinician's voice, not template copy.
WOW: Drop caps read instantly as 'someone art-directed this' rather than 'this is a Squarespace template.' It's the single cheapest move that makes body copy feel like a magazine profile instead of a brochure page, and it costs zero extra assets.
FIT: Applied only to the About page's biographical narrative (read by patients/parents who have time), never to the homepage's 45-second referral scan — so it adds warmth without slowing down the office-manager audience. Uses the already-approved orange-ink and Fraunces SOFT axis, no new colors or fonts.
IMPL: In src/app/about/page.tsx, wrap the bio's opening paragraphs in a `.editorial-intro` container. Add to globals.css: `.editorial-intro > p:first-of-type::first-letter { font-family: var(--font-display); font-variation-settings: 'SOFT' 100, 'opsz' 144; font-style: italic; font-weight: 480; color: var(--color-orange-ink); float: left; font-size: 4.2em; line-height: 0.82; padding: 0.03em 0.1em 0 0; }` then progressively enhance with `@supports (initial-letter: 3) { .editorial-intro > p:first-of-type::first-letter { initial-letter: 3 2; float: none; } }` for browsers that support the newer, better-aligned property.
RISK: Low — pure CSS, no layout shift risk since it's scoped to one paragraph; must sanity-check line-wrap on mobile widths so the float doesn't collide with short lines.
URLS: https://css-tricks.com/snippets/css/drop-caps/, https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/hanging-punctuation

### Pull-quote component with hairline rule + hanging punctuation  [quick-win · hours]
WHAT: A centered, oversized italic Fraunces statement — flanked by a 44px hairline rule above it and a small-caps attribution line below — breaks up long-form pages the way a magazine pull quote breaks a feature article. Uses real, already-approved copy (e.g. 'The same clinician answers your call, verifies your coverage, and sits across the screen every visit') — never an invented testimonial.
WOW: Pull quotes are the fastest way to make a page look 'edited' rather than 'filled in.' The hairline rule + hanging opening quote mark is a tell designers screenshot because it shows someone thought about optical alignment, not just font-size.
FIT: Content is drawn from Katie's own existing bio/positioning copy, not a fabricated testimonial or stat — satisfies the no-invented-facts rule while still reading as a confident editorial statement. Rose/olive hairline keeps it in the approved accent palette.
IMPL: New src/components/ui/PullQuote.tsx used on /about and /services. CSS: `.pull-quote{max-width:34ch;margin:3.5rem auto;text-align:center}.pull-quote::before{content:'';display:block;width:44px;height:1px;background:var(--color-olive);margin:0 auto 1.4rem}.pull-quote p{font-family:var(--font-display);font-style:italic;font-variation-settings:'SOFT' 100,'opsz' 60,'WONK' 1;font-size:clamp(1.5rem,1.1rem + 1.4vw,2.1rem);line-height:1.32;color:var(--color-brown);hanging-punctuation:first last;text-wrap:pretty}.pull-quote cite{display:block;margin-top:1rem;font-family:var(--font-sans);font-style:normal;font-size:11.5px;letter-spacing:.18em;text-transform:uppercase;color:var(--color-muted)}`. Requires adding `WONK` to the Fraunces axes array in layout.tsx.
RISK: hanging-punctuation is Safari-only per MDN/caniuse — treat as progressive enhancement, not a dependency; verify the quoted copy is already client-approved before publishing.
URLS: https://developer.mozilla.org/en-US/docs/Web/CSS/hanging-punctuation, https://caniuse.com/css-hanging-punctuation

### WONK-axis warmth on hover for accent words and links  [quick-win · hours]
WHAT: Italic accent words (the `.accent-word` class already used in headings) and text links very subtly 'relax' on hover — the letterforms shift from their crisp default into Fraunces' slightly wonky, hand-set alternate forms over ~300ms, then settle back. It reads as a barely-perceptible warmth, not a gimmick.
WOW: Nobody else in the dietitian/healthcare-marketing space is animating variable-font axes on hover — it's a 2025-era technique still rare outside type-foundry demo sites and design-studio portfolios, so it's an instant 'how did they do that' moment for anyone who hovers slowly.
FIT: It's motion at the level of ink, not layout — fully inside the Level 2 budget (CSS-only, no new JS), and subtle enough to never read as 'playful/bakery.' WONK is literally the axis Fraunces' own type foundry designed for exactly this kind of restrained warmth.
IMPL: Add `WONK` to the `axes` array for Fraunces in src/app/layout.tsx (`axes: ['SOFT','opsz','WONK']`). In globals.css: `.accent-word{font-variation-settings:'SOFT' 80,'WONK' 0;transition:font-variation-settings 320ms var(--ease-soft)}.accent-word:hover,.group:hover .accent-word{font-variation-settings:'SOFT' 100,'WONK' 1}`. Apply the same transition to nav links and the 'More About Katie' text-links already in AboutBlock.tsx.
RISK: font-variation-settings animations must stay on GPU-friendly axes (SOFT/WONK are fine); test that layout doesn't reflow since Fraunces' wonk alternates can shift glyph width slightly — pin with font-variant-ligatures/tabular where needed.
URLS: https://fonts.adobe.com/fonts/fraunces-variable, https://www.npmjs.com/package/@fontsource-variable/fraunces

### Folio-numbered steps and FAQ, print-style running rules  [quick-win · hours]
WHAT: The four numbered Steps and the FAQ accordion get treated like a print folio: tabular '01 —— 04' style numerals with a hairline rule stretching to the card edge, tracked micro-labels, echoing how a magazine numbers pages and sections rather than using generic bullet circles.
WOW: It's a small, consistent system (not a one-off flourish) — the kind of detail that shows up in an awwwards case-study screenshot captioned 'look at their numbering system.' Consistent folio treatment across Steps + FAQ makes the whole site feel like one edited document.
FIT: Reinforces 'clinician talking to a colleague' — numbered, procedural, unfussy — while still being visually distinctive. No new colors; uses existing olive and border-strong tokens.
IMPL: In Steps.tsx and FaqAccordion.tsx, replace the current number/icon markers with a `.step-folio` element: `.step-folio{display:flex;align-items:center;gap:.75rem;font-family:var(--font-sans);font-variant-numeric:tabular-nums;font-size:12px;letter-spacing:.22em;color:var(--color-olive)}.step-folio::after{content:'';flex:1;height:1px;background:var(--color-border-strong)}`. Render numbers as `String(i+1).padStart(2,'0')` so it's '01', '02' not '1', '2' — the leading zero is what reads as 'print,' not 'web app.'
RISK: Low — purely presentational change to existing markup, no new dependencies.

### Old-style figures and small caps for credentials, phone numbers, dates  [quick-win · hours]
WHAT: Katie's credential line ('REGISTERED DIETITIAN (RD)'), phone numbers, and any inline years switch from default all-caps/lining-figure typesetting to true small caps and old-style (text) figures — the same refinement a well-set print CV or medical directory entry would use.
WOW: It's invisible to a casual visitor and unmistakable to anyone with a type background — the kind of print-quality detail (OpenType features actually engaged, not faked with CSS uppercase + smaller font-size) that separates a designed site from a themed one.
FIT: Reads as precise and clinical-grade craft rather than decorative — exactly the 'business professional' register Katie asked for. Source Sans 3 ships real smcp/onum OpenType features so this isn't a hack.
IMPL: In globals.css add utility classes: `.small-caps{font-variant-caps:small-caps;font-feature-settings:'smcp' 1,'c2sc' 1;letter-spacing:.02em}.oldstyle-figures{font-variant-numeric:oldstyle-nums proportional-nums}.tabular{font-variant-numeric:tabular-nums}`. Apply `.small-caps` to the credential line in AboutBlock.tsx and Hero.tsx (replacing the current text-transform:uppercase treatment), `.oldstyle-figures` to inline body-copy numerals, and keep `.tabular` on the FACTS card and step numbers where columns need to align.
RISK: Confirm Source Sans 3's variable font as loaded via next/font actually exposes smcp/onum tables (some subset builds strip optional features) — verify by rendering a test string before shipping site-wide.
URLS: https://fontfyi.com/blog/variable-fonts-complete-guide/

### Restrained duotone treatment for supporting/environment photography  [medium · hours]
WHAT: Non-portrait supporting photography (e.g. a texture/environment image behind StatementBands, or a secondary image on the Services page) is rendered in a brown-to-rose duotone tied to the palette, rather than full color — giving the site one consistent, branded photographic voice for anything that isn't Katie's face.
WOW: Duotone photography is a signature move of premium editorial and design-studio sites right now precisely because it makes stock or supporting imagery feel bespoke and unified with the brand palette instead of generic.
FIT: Deliberately scoped away from Katie's actual headshot — a referring physician's office needs to see a real, trustworthy clinician photo, not a stylized one, so duotone stays reserved for atmosphere/supporting images only. Uses only the approved brown/rose/cream tokens, nothing new.
IMPL: Wrap the target `<img>` in a `.duotone-photo` container: `.duotone-photo{position:relative;isolation:isolate;overflow:hidden}.duotone-photo img{filter:grayscale(1) contrast(1.05);mix-blend-mode:luminosity;width:100%;display:block}.duotone-photo::before{content:'';position:absolute;inset:0;background:linear-gradient(160deg,var(--color-brown) 0%,var(--color-rose) 100%);mix-blend-mode:color;z-index:1;pointer-events:none}`. Apply only in StatementBands.tsx or a new Services page environment shot — never on Hero.tsx or AboutBlock's headshot slot.
RISK: mix-blend-mode:color needs a solid (non-transparent) background behind it to render consistently across browsers — test in Safari specifically, which handles blend-mode stacking contexts differently.
URLS: https://jmperezperez.com/blog/duotone-using-css-blend-modes/, https://codepen.io/meowwwls/pen/qBBWYJJ

### Scroll-triggered SOFT-axis 'settle' on headlines  [medium · day]
WHAT: As each section heading scrolls into view (using the IntersectionObserver already wired up in Reveal.tsx), the Fraunces SOFT axis animates from a crisper 0 up to the site's signature 80 over ~900ms while the heading also fades/lifts in — so headlines don't just appear, their ink visibly 'settles' into its warm, soft-serif form.
WOW: This ties the site's whole visual thesis — warmth, ink, softness — directly to the one element every page is built around: the headline. It's a more sophisticated, on-brand alternative to a generic fade-up, and variable-font-axis scroll animation is still rare enough in production sites (mostly seen on type foundry/studio demo pages) that it reads as genuinely bespoke.
FIT: Runs entirely on the compositor via font-variation-settings transitions — no WebGL, no scroll library, stays comfortably inside the CSS+light-JS motion budget, and the effect is subtle (a font-weight/softness shift, not a bounce or spin) so it never tips into 'flashy.'
IMPL: Extend Reveal.tsx's existing observer to toggle a `.is-settled` class on h2/h3 children instead of (or in addition to) opacity. In globals.css: `h2,h3{font-variation-settings:'SOFT' 20,'opsz' 40;transition:font-variation-settings 900ms var(--ease-soft)}h2.is-settled,h3.is-settled,.is-visible h2,.is-visible h3{font-variation-settings:'SOFT' 80,'opsz' 72}`. Reuse the same IntersectionObserver instance already powering Reveal to avoid adding a second observer.
RISK: Must confirm Fraunces' opsz axis is loaded across the full 9–144 range (axes:['SOFT','opsz','WONK'] in next/font config) or the opsz jump will be a no-op; also verify no cumulative-layout-shift from the axis change by checking computed glyph metrics stay close enough not to reflow.
URLS: https://www.npmjs.com/package/@fontsource-variable/fraunces

### Butterfly-wing art-directed photo crop  [ambitious · day]
WHAT: Katie's hero or about-page photo is cropped with an asymmetric, wing-curved edge — one corner softly scalloped in the shape of the butterfly mark's wing line — instead of a plain rectangle or uniform border-radius, so the site's line-art motif and its one real photograph visually rhyme.
WOW: Art-directed, non-rectangular photo crops are a hallmark of 2025-2026 premium editorial/portfolio sites (a step beyond the now-common 'blob' shape because it's drawn from the site's own mark rather than a generic organic shape) — it's the kind of one detail that anchors a whole design language and gets pointed to in a screenshot.
FIT: Reuses the already-approved ButterflyMark SVG asset — no new imagery, no new motif, just applying the existing line-art curvature to the photo frame. Kept to a single, restrained corner cut rather than a full silhouette so it stays 'business professional,' not twee.
IMPL: Extract one wing curve from src/components/ui/ButterflyMark.tsx's SVG path data and simplify it into a `clip-path: path('M0,0 ...')` (or an equivalent CSS `clip-path` polygon approximation) applied to the Hero and About headshot's image wrapper. Ship a lower-risk fallback in the same commit — an asymmetric border-radius like `border-radius: 140px 28px 140px 28px` — behind a feature check, so if the traced path looks off on review it degrades gracefully to a still-distinctive but simpler shape.
RISK: Highest-craft, highest-risk idea here: a poorly traced clip-path can look accidental rather than intentional, and clip-path doesn't respond to art direction per-breakpoint automatically — needs distinct path values (or the border-radius fallback) at mobile widths where the photo crops narrower.

## v2:6057ab176d89e30d5746fa25b4d22bdae69fdaa5cd22101b2b53908c95b97246  (8 ideas)

### Signature line-draw: the butterfly mark ink-strokes itself in  [quick-win · hours]
WHAT: The existing ButterflyMark SVG (already used static/faded in AboutBlock and as the footer's small mark) draws itself stroke-by-stroke like a pen sketching it, the instant it scrolls into view — wings and body appearing as continuous ink lines rather than fading in as a flat shape. Runs once in AboutBlock's big decorative mark (currently a static 0.16-opacity olive butterfly) and again, larger, in a new footer moment.
WOW: It's the one truly bespoke asset on the site — animating it as hand-drawn ink turns a placeholder-feeling decoration into the site's signature moment, the kind of detail visitors screenshot. Costs nothing extra to load since the SVG is already inline.
FIT: Reads as clinical precision and craft, not decoration-for-its-own-sake — a single quiet flourish, not a mascot. Pure CSS/SVG, zero new dependencies, trivially respects reduced-motion.
IMPL: Each <path>/<line> in src/components/ui/ButterflyMark.tsx gets pathLength="1" plus a CSS class; keyframes animate stroke-dashoffset 1→0 with stroke-dasharray:1, staggered ~80ms per path via nth-child. Trigger with the same IntersectionObserver pattern already in src/components/ui/Reveal.tsx (add an .is-inview class to the wrapping <g>) so it fires once, fails safe with prefers-reduced-motion (draw state jumps to end, matching the current Reveal fallback). No new library.
RISK: Low — additive CSS/SVG only, existing IO hook pattern already proven in the codebase.

### A whisper of film grain across the brown bands and hero  [quick-win · hours]
WHAT: A near-invisible, fixed-position paper/film grain texture sits over the whole page at ~3-4% opacity, most noticeable where it breaks up the flattest color fields — the brown StatementBands, the hero's gradient overlay, the footer. It never reads as 'a texture,' it just makes the deep brown feel like ink on warm paper instead of a flat CSS color.
WOW: This single trick is what separates 2025-2026 premium/editorial sites from template-brochure sites — it's everywhere in current awwwards-tier work precisely because it's cheap, invisible individually, and expensive-feeling in aggregate.
FIT: Completely silent — no motion, no attention-grabbing, just material depth under the existing warm-clinical palette. Reinforces 'quiet professional,' not 'influencer.'
IMPL: Inline SVG feTurbulence noise (baseFrequency ~0.8, numOctaves 3) encoded as a data-URI background-image on a fixed, full-viewport, pointer-events:none div added once in src/app/layout.tsx; mix-blend-mode: overlay (soft-light on the cream sections to avoid darkening body text), opacity 0.035-0.05. Zero JS, zero extra network request, adds well under 1KB to CSS/HTML since it's inlined.
RISK: Low — must verify it doesn't shift Lighthouse contrast/perf scores; test blend-mode doesn't muddy text on cream vs brown sections separately.
URLS: https://www.freecodecamp.org/news/grainy-css-backgrounds-using-svg-filters/, https://css-tricks.com/grainy-gradients/

### Footer becomes the destination, not the exit  [quick-win · hours]
WHAT: The current footer wordmark ('Monarch,' currently text-2xl) grows into a full-bleed oversized Fraunces headline — something like clamp(4rem, 3rem+8vw, 11rem) — with the butterfly mark drawn in large and faint behind/beside it via mix-blend-mode. The nav links and disclaimer sit below as a quiet, structured close, but the last thing a visitor sees before leaving is one huge, warm, confident wordmark.
WOW: 'Footer as destination' is a defining 2025-2026 award pattern — most sites waste the last screen on link soup. Turning it into a single calm type statement is the kind of ending that makes a site feel authored rather than assembled.
FIT: Zero new copy or claims — same wordmark, same license/disclaimer text already there, just given room to breathe. No risk of overpromising language since nothing changes but scale.
IMPL: Edit src/components/layout/Footer.tsx: replace the fixed text-2xl wordmark with a fluid clamp() size (Fraunces 440, same as hero treatment already used for h1), keep it in the existing cream-on-brown palette; place a large (400-600px) ButterflyMark at low opacity with mix-blend-mode:screen behind the wordmark, absolutely positioned. Optional: apply the same stroke-draw animation from idea #1, triggered on footer scroll-into-view.
RISK: Low — layout-only change to one existing component, no new state or JS beyond the already-used Reveal/IO pattern.

### The curved statement band inverts as you cross it  [medium · day]
WHAT: The existing curved brown StatementBand (white lip, 'Real nutrition care, without the waiting room') gets a scroll-scrubbed moment: as the section crosses the viewport, the curve's radius subtly tightens and the orange caps band beneath it very slightly scales/brightens — a soft, controlled 'the page takes a breath' beat exactly at the color inversion point that's already baked into the design (cream → brown → orange → cream).
WOW: Scroll-tied color/shape transitions at existing structural seams are what make a scroll feel directed rather than just scrolled — and this section is already the natural inversion point in the layout, so the effect reinforces structure that's already there instead of adding a new gimmick.
FIT: Motion is progress-linked and reversible, not decorative animation running on a timer — stays in the 'clinician talking to a colleague' register because it's structural, not flashy.
IMPL: CSS scroll-driven animations (animation-timeline: scroll(), animation-range) on src/components/sections/StatementBands.tsx's curve <span> and the orange band: e.g. `border-radius` and a 1.0→1.03 `scale` interpolated across the section's scroll range. Wrap in `@supports (animation-timeline: scroll())` with the current static state as the fallback (Chrome/Edge 115+, Safari 18+; Firefox falls back gracefully to the existing static look — true progressive enhancement, no JS needed at all).
RISK: Medium — scroll-timeline is CSS-native but young; needs `@supports` guarding and manual testing across Safari 18 vs older Safari/Firefox to confirm the fallback is clean, not janky.
URLS: https://developer.mozilla.org/en-US/docs/Web/CSS/Guides/Scroll-driven_animations, https://developer.mozilla.org/en-US/docs/Web/CSS/Guides/Scroll-driven_animations/Timelines

### Service tile photos wipe into place instead of just fading  [quick-win · hours]
WHAT: The three ServiceTiles cards already hover-scale their photos; add a one-time entrance where each photo wipes in from behind a soft clip-path edge (like a card being slid out of a sleeve) as the tile scrolls into view, instead of the current plain fade/rise.
WOW: It's the same 'reveal, don't just appear' idea driving most current image-forward award sites, applied to content already on the page — no new photography needed, just a more considered way of introducing images that are already there.
FIT: Subtle, one-directional, fires once — reads as polish on real clinical-service photography, not a flashy transition. Keeps audience #1 (an office manager skimming in 45 seconds) unslowed since it's fast (~600-700ms) and doesn't delay the 'Learn more' CTA.
IMPL: In src/components/sections/ServiceTiles.tsx, wrap each card's image div in a class using clip-path: inset(0 0 0 100%) → inset(0) as the reveal state (or `animation-timeline: view()` with `animation-range: entry 0% entry 40%` for pure-CSS scroll-linking), swapped on the same .is-inview class the Reveal wrapper already adds. No new component — extends the existing Reveal/IO pattern already wired to these cards.
RISK: Low — same trigger mechanism already in use; just needs clip-path easing tuned so it doesn't feel like a loading spinner.
URLS: https://motion.dev/examples/react-scroll-image-reveal, https://tympanus.net/codrops/2026/03/11/svg-mask-transitions-on-scroll-with-gsap-and-scrolltrigger/

### The three steps pin and stack as you scroll past them  [medium · day]
WHAT: Instead of the current side-by-side 1/2/3 grid, on desktop the Steps section becomes a short pinned sequence: each numbered card holds sticky position center-screen while the next one scrolls up and gently scales/dims the previous card behind it, so 'Reach out → Verify coverage → First visit' feels like a guided walkthrough rather than a static list.
WOW: Sticky-stacking cards are a hallmark scroll-storytelling pattern on premium product sites — applied to a 3-step process it turns an intake flow into something that feels considered and sequential, matching the actual chronology of what happens.
FIT: Only 3 items, short pin distance — this stays a 5-8 second beat, not a long hijacked scroll, so it never traps or annoys the office-manager audience. Falls back cleanly to the current grid on mobile and under reduced-motion.
IMPL: In src/components/sections/Steps.tsx: on lg+ viewports, give the section a taller scroll track (e.g. 240vh) and each step card `position: sticky; top: 30vh` with an incrementing z-index; use `animation-timeline: view()` (or a lightweight scroll-position listener reusing the existing IO infra) to scale prior cards down to ~0.92 and drop opacity to ~0.6 as the next one arrives. Mobile keeps today's simple 3-column/stacked grid — no pin, no dependency added.
RISK: Medium — sticky+stacking interactions are easy to get subtly janky (scroll-jank, wrong z-index stacking, awkward mobile fallback) and need real device testing; keep the pin distance short to limit blast radius.

### Native page-to-page crossfade via the View Transitions API  [medium · day]
WHAT: Navigating between Home → Services → About → Contact gets a soft, browser-native crossfade-with-8px-rise instead of the default hard cut, and the sticky header stays visually anchored across the transition (it doesn't blink or reflow) — the site starts feeling like one continuous, considered experience instead of seven separate static pages.
WOW: This is the single biggest 'feels expensive' upgrade available for the least visual noise: it's invisible as a described feature but immediately felt on every click, and it's now a first-class, officially documented Next.js 16 capability (no animation library, no bundle-size cost).
FIT: Completely content-neutral — no new claims, no new visual elements, purely a navigation-feel upgrade. Respects prefers-reduced-motion by design (disable animation-duration under the media query, per Next's own guidance) and stays under the JS budget since it's a native browser API, not a library.
IMPL: Set `experimental: { viewTransition: true }` in next.config.ts; wrap route content in React's `<ViewTransition>` (ships in the React canary Next 16 already uses) with a same-route crossfade (`enter="auto" share="auto"`) plus directional `nav-forward`/`nav-back` on the header nav's <Link>s in src/components/layout (give the header a stable `viewTransitionName` so it doesn't re-animate). Add the crossfade keyframes to globals.css alongside the existing `--ease-soft` token, and the `prefers-reduced-motion` override block from Next's own guide.
RISK: Medium — experimental Next.js flag; needs a full click-through QA pass (all 7 pages + 404 + blog template) to confirm no flash-of-unstyled-header or SSG/RSC interaction issues before shipping, and Safari support is less polished than Chromium's.
URLS: https://nextjs.org/docs/app/guides/view-transitions

### Hero photo breathes with a whisper of parallax on load-scroll  [medium · hours]
WHAT: As a visitor first scrolls past the hero, the kitchen photo drifts very slightly slower than the page (a few percent scale + translate) rather than moving in lockstep, before the InsuranceStrip band arrives — the classic 'the photo has depth' cue used on nearly every premium hero in 2025-2026, kept to a couple of pixels of motion.
WOW: It's the smallest-footprint version of scroll-depth storytelling and it lands exactly where audience #1 forms their first impression — the hero is the 45-second moment, and parallax reads as 'considered production,' not gimmick, when this restrained.
FIT: Must not touch anything that affects LCP — the Hero component is explicitly marked 'no entrance motion (LCP rule)' in the code today, so this has to be scroll-linked only (never delays paint) and capped at a couple of percent movement so it reads as depth, not a moving background.
IMPL: Pure CSS on the existing <Image> wrapper in src/components/sections/Hero.tsx: `animation-timeline: scroll(root)` with `animation-range: 0 40vh` driving `transform: scale(1.0→1.06) translateY(0→12px)` — compositor-only properties, no layout thrash, guarded in `@supports (animation-timeline: scroll())` so browsers without support just render the static hero exactly as today (which already meets the LCP budget).
RISK: Low-medium — must verify with Lighthouse that the animation itself doesn't get flagged as a CLS/LCP contributor (compositor-only transform should be safe, but re-measure after adding).
URLS: https://dev.to/nickbenksim/creating-complex-scroll-driven-animations-with-pure-css-in-2026-17l

## v2:0d41a99a81abc38bb14d53e593bba89f541fae12b6db6cf51d1d629228472653  (7 ideas)

### Myth vs. Fact flip cards  [quick-win · hours]
WHAT: A row of 4-6 cards on the home page (between StatementBands and ServiceTiles) or on /blog, each showing a common referral-relevant misconception on the front in Fraunces italic ("Insurance won't cover a dietitian"), and on click/tap flips 180deg to reveal the clinical correction in Katie's voice on the back (brown card, cream text, rose-ink label). Office managers and parents skim exactly this kind of quick-hit reassurance.
WOW: It's the single cheapest way to make the site feel alive without touching the clinical tone -- a physical 'turn the card over' gesture that rewards curiosity in under a second, which is exactly the 45-second attention window Katie's #1 audience gives a site.
FIT: Zero data collection, no invented facts (content is Katie-authored corrections, not stats), stays in the existing card/hairline visual language (18px radius, brown-tinted shadow) already defined in the build spec -- just adds a Y-axis rotation to a card that already exists conceptually as ServiceTiles.
IMPL: New MythFactCard.tsx client component: two stacked divs inside a perspective:1200px wrapper, .card-inner has transform-style:preserve-3d and transitions transform 500ms cubic-bezier(.22,1,.36,1); toggling a `flipped` class (click, not hover, for mobile) sets rotateY(180deg); back face has backface-visibility:hidden and pre-rotated rotateY(180deg). Wrap in existing Reveal.tsx for scroll-in. Wrap the whole flip transition in @media (prefers-reduced-motion:no-preference) with an instant-swap fallback (opacity crossfade) for reduced motion, matching the FAQ accordion's existing motion-safe pattern.
RISK: Content must be Katie-approved per card (no invented claims); needs a real click target (not hover-only) for touch, and preserve-3d/backface-visibility needs the -webkit- prefix noted in sources for older Safari.
URLS: https://www.sliderrevolution.com/resources/css-flip-cards/, https://wpdean.com/css-flip-cards/

### Corporate lunch-and-learn topic picker  [quick-win · hours]
WHAT: On /services/corporate-nutrition, replace the flat offerings list with a grid of topic cards ("Grocery Store Literacy," "Desk-Lunch Reset," "Reading a Nutrition Label in 60 Seconds") that expand in place on click to show format + typical length, then a quiet 'Ask about this for your team' link that scrolls to the existing 3-field contact form -- never a booking widget.
WOW: Gives an HR contact something to actually browse and picture instead of a wall of text -- the artifact of 'shopping' a menu is what makes them remember and forward the page internally.
FIT: No new data field is collected -- the expand-in-place card still funnels to the same name/email/phone form. It's B2B-appropriate restraint: informative, not gamified, and keeps the site's clinician tone since each card is a real service description, not marketing fluff.
IMPL: Extend ServiceTiles.tsx pattern (or a new TopicPicker.tsx) using CSS grid-template-rows: 0fr / 1fr transition on the detail panel (the modern height-auto-transition trick) triggered by a button toggling aria-expanded, so it's accessible via keyboard and doesn't need JS height measurement. Real example of this service category from a working dietitian practice for tone calibration.
RISK: Low -- mostly copywriting risk (needs Katie's actual topic list, not invented ones) and making sure the grid-rows transition trick is applied consistently (some browsers still need overflow:hidden on the wrapper).
URLS: https://www.julienutrition.com/services/workplace-wellness/lunch-and-learn/

### Build-a-Balanced-Plate interactive diagram  [medium · day]
WHAT: A generic, brand-colored plate diagram (SVG wedges: vegetables, protein, whole grains, healthy fats) on /services/individual-nutrition-counseling, styled with the cream/brown/orange/rose palette instead of a stock MyPlate green/red. Clicking a wedge highlights it and swaps a caption panel with a one-line clinical note ("Half the plate, any vegetable, any prep") -- modeled on Harvard's Healthy Eating Plate interaction pattern but redrawn in Katie's visual language.
WOW: It's the one interactive piece a patient (not just an office manager) would actually poke at before a first appointment -- it visually proves 'this is a clinician's site,' not a wellness-influencer site, while still being tactile.
FIT: Content is public-domain nutrition guidance (same territory as Harvard's own public plate, USDA MyPlate) so there's no invented-fact risk; wedges are decorative SVG, no photos, no tracking, no personalization stored -- purely stateless click-to-reveal.
IMPL: Static SVG (`<path>` wedges drawn once, no canvas) in a new BalancedPlate.tsx client component; each `<path>` is a `<button>`-wrapped focusable region with `aria-pressed`; click toggles an `active` class that changes fill to orange/rose tint and cross-fades the adjacent caption div (opacity + 8px translate, same easing as existing Reveal.tsx). Verified working reference for the underlying interaction model (click-to-reveal wedge captions) before restyling in Katie's palette.
RISK: Needs Katie to confirm the exact wedge copy so nothing reads as invented clinical claims; SVG wedge hit-targets need care on mobile (min 44px touch target per wedge, may need invisible larger hit-path).
URLS: https://nutritionsource.hsph.harvard.edu/healthy-eating-plate/

### "What a telehealth visit looks like" scroll-linked walkthrough  [medium · day]
WHAT: Upgrade the existing numbered Steps.tsx into a horizontal (desktop) / vertical (mobile) journey with a thin connecting line that visibly draws itself as the visitor scrolls the section into view, plus each step is click-to-expand for a 1-2 sentence detail (what happens, what to bring, how long). Placed on the home page and echoed in condensed form on /contact so a nervous first-time patient can preview the visit before booking.
WOW: It directly answers the anxiety Katie's audience #2 (patients) has about telehealth -- 'what actually happens on the call' -- with a moment of motion that feels premium (the line drawing itself) rather than a static list, matching the calm, clinical, unhurried tone.
FIT: No video, no chat, no scheduling widget -- purely a CSS-driven reveal of static, Katie-authored copy. Uses the brand's own butterfly/hairline visual vocabulary (numbered Fraunces italic numerals already spec'd) rather than a generic progress bar.
IMPL: Add an SVG `<path>` connector behind the existing numbered steps; use `stroke-dasharray` = path length, `stroke-dashoffset` animated via `animation-timeline: view()` + `animation-range: entry 0% cover 60%` (the view-progress-timeline technique) so the line fills as the section scrolls into the viewport -- zero JS, compositor-thread only. Each step's detail panel uses the same grid-template-rows 0fr/1fr disclosure as the topic picker idea, toggled by click. Wrap in `@media (prefers-reduced-motion: no-preference)`; fallback is the line simply rendered fully-drawn at rest (exactly the pattern this spec's existing motion-safe FAQ rotation already follows).
RISK: Firefox has scroll-driven animations behind a flag as of this research pass, so the fallback (statically drawn line) must look intentional, not broken -- keep the reduced-motion/no-support state as the real design, not an afterthought.
URLS: https://www.joshwcomeau.com/animation/scroll-driven-animations/, https://developer.mozilla.org/en-US/docs/Web/CSS/Guides/Scroll-driven_animations

### Seasonal produce explorer  [medium · day]
WHAT: A tabbed widget (Spring / Summer / Fall / Winter) showing a grid of ~10-12 produce icons per season with a one-line dietitian tip on hover/tap ("Corn: pairs well with a lean protein for a balanced plate"). Lives on /blog as a standalone evergreen resource post or a sidebar widget, giving Katie recurring, shareable, non-clinical content that still reads as her voice.
WOW: It's the kind of thing a parent or patient actually bookmarks and returns to month over month -- recurring reasons to revisit the site is rare for a solo-practice dietitian site, and it positions Katie as a real resource rather than a lead-gen funnel.
FIT: All content sourced from public USDA/seasonal-guide data (not invented stats), stored as a static client-side array (no fetch, no accounts, no location tracking) -- explicitly the opposite of the zip-code-based seasonal apps researched, which would require data collection Katie's constraints forbid.
IMPL: New SeasonalProduce.tsx client component: a `role="tablist"` of 4 season buttons toggling a CSS grid of produce cards via a simple useState index (no router state, no persistence) -- same card treatment (18px radius, cream/white surface) as ServiceTiles. Tooltip on hover uses a native `title`-replacement pattern with a small absolutely-positioned caption div shown on `:hover`/`:focus-visible` for keyboard parity. Content basis verified against USDA SNAP-Ed and FoodPrint's public seasonal guides, generalized (not zip-code-specific) to stay data-free.
RISK: Needs Katie to sign off on the seasonal list generalized for her MO/NJ service area rather than nationally averaged, to avoid an implied but wrong regional claim.
URLS: https://snaped.fns.usda.gov/resources/nutrition-education-materials/seasonal-produce-guide, https://foodprint.org/eating-sustainably/seasonal-food-guide/

### Signature scroll-drawn butterfly mark  [ambitious · hours]
WHAT: The existing ButterflyMark.tsx line-art logo 'draws itself' in a single continuous stroke the first time it scrolls into view near the hero or About section -- a 1-2 second moment where the outline traces from wingtip to wingtip before settling into the static mark used everywhere else.
WOW: It's a genuine signature moment -- the kind of detail that makes someone screenshot the site or mention it to a colleague -- built entirely from an asset Katie already approved (the butterfly mark), so it reinforces brand rather than adding a new visual element.
FIT: Fires once, is purely decorative brand reinforcement (no content, no data), and is exactly the 'quiet, elevated, not-influencer' register Katie asked for -- restrained motion on a single trusted symbol rather than a flashy hero animation.
IMPL: Convert ButterflyMark.tsx's paths to have `pathLength="1"` (normalizes stroke-dasharray math across path segments), then `stroke-dasharray:1; stroke-dashoffset:1` animated to `0` via `animation-timeline: view()` scoped to the mark's own visibility (`animation-range: entry 20% entry 80%`) -- same view-progress-timeline primitive as the telehealth walkthrough idea, so it's one technique reused twice for consistency and less code. `fill` fades in only after the stroke completes (a second keyframe on the same timeline). Static SVG fallback (already in place) for reduced-motion and non-supporting browsers.
RISK: Multi-path SVGs need each subpath's `pathLength` normalized or the draw speed looks uneven across wings; test the specific butterfly artwork's path count before committing to a single continuous-stroke illusion (may need to draw wings in a deliberate sequence instead of simultaneously).
URLS: https://www.joshwcomeau.com/animation/scroll-driven-animations/

### Child & Teen page: quiet-play produce garden  [ambitious · days]
WHAT: On /services/child-teen-nutrition only, a small grid of simple flat-illustrated fruit/veg characters (still in the brown/cream/orange/rose palette, not cartoon-bright) that gently wiggle or bounce on hover/tap and reveal a one-line kid-friendly, non-clinical fact bubble ("Blueberries are basically tiny superhero snacks") -- confined to this single page so the rest of the site stays fully clinical.
WOW: It's the one page where a parent wants to feel Katie 'gets kids' without the whole brand tipping into influencer territory -- a contained moment of warmth that a parent scrolling on their phone during a pediatrician visit will actually pause on and remember.
FIT: Scoped to a single page by design intent (matches the build spec's note that /services/child-teen-nutrition is a standalone ad-landing page with its own tone discipline); still zero data collection, no quiz, no name-entry personalization -- just decorative hover facts.
IMPL: New ProduceGarden.tsx client component, CSS-only wiggle via `@keyframes wiggle { 0%,100%{transform:rotate(0)} 25%{transform:rotate(-4deg)} 75%{transform:rotate(4deg)} }` triggered on `:hover`/`:focus-visible` (400ms, motion-safe only), each character is an inline SVG (custom-illustrated, not stock clipart, to avoid clashing with the site's line-art brand) with a small speech-bubble caption that fades in via the same Reveal.tsx opacity pattern used elsewhere.
RISK: Highest effort item here -- needs custom illustration work (can't use stock kids'-nutrition clipart without breaking brand cohesion), and needs a firm content review so playful facts don't drift into unverified nutrition claims for children specifically.
URLS: https://www.eatright.org/for-kids

## v2:69c9ee737a418727d13dbd6a7b1c72b698b96ae0069c54ae4e4eae2fabd24706  (8 ideas)

### Butterfly mark wing flutter  [quick-win · hours]
WHAT: Hover or keyboard-focus the header's line-art butterfly logo and its four wing paths tilt outward a few degrees and settle back — a single quick, quiet flutter, not a loop. On page load it stays perfectly still (no idle animation, no attention-stealing).
WOW: It's a signature move because it's literal brand animation — the one graphic element unique to Katie's identity comes alive for a fraction of a second exactly when someone's eye is on it (hovering toward Home). Nobody else's dietitian-practice nav does this; it costs nothing in JS and reads as considered rather than decorative.
FIT: Restrained, single-beat, over in ~400ms — clinician-colleague tone, not influencer bounce. It's polish on the one asset that already exists as a mark, not a new gimmick layered on.
IMPL: In ButterflyMark.tsx, wrap the two left-side paths in <g className="wing-left"> and two right-side paths in <g className="wing-right">, each with transform-origin: 16px 15px (the body line). In globals.css: header a:hover .wing-left, header a:focus-visible .wing-left { animation: flutter-left .4s var(--ease-soft); } with a 2-3deg rotate-and-return keyframe (mirrored for wing-right). Gate the whole thing behind @media (prefers-reduced-motion: no-preference) and (hover: hover) so touch devices just get the static mark. Zero added JS.
RISK: SVG group transform-origin math needs a couple of visual passes to land on the wing hinge point convincingly; if it looks like the whole butterfly wobbles instead of the wings hinging, it'll read as broken rather than alive.

### Scroll-progress rail under the header  [quick-win · hours]
WHAT: A hairline (2-3px) bar spanning the full width directly under the sticky header, empty on page load, that fills from cream through a brown-to-orange gradient as the visitor scrolls down long pages (About, Services detail, Blog post). Native browser scrollbar-adjacent, not a toast or a floating widget.
WOW: It's the kind of quiet, confidence-signaling chrome you see on long-form editorial and premium SaaS docs sites in 2025-2026 — it tells a physician's-office reader 'this is a considered, complete page' without asking for any attention. Built on the browser's native scroll-driven-animation timeline, so it costs zero JS and runs off the main thread.
FIT: Utterly quiet, purely functional, no motion library, and invisible to anyone who isn't scrolling a long page — exactly the 'far quieter than an influencer site' register.
IMPL: Add a fixed 2-3px <div> right below <header>, styled with a linear-gradient(90deg, var(--color-orange), var(--color-rose)) and `transform-origin: left; animation: grow-rail linear; animation-timeline: scroll(root);` where @keyframes grow-rail sets transform: scaleX(0) to scaleX(1). No animation-timeline: scroll() support (Firefox stable) just leaves the bar static at 0 width, which is an acceptable no-op fallback — wrap in @supports (animation-timeline: scroll()) so unsupported browsers don't render an empty bar at all.
RISK: Firefox stable doesn't support scroll() timelines yet (Chrome/Edge/Safari do) — must confirm the @supports fallback actually hides the element rather than showing a stuck full-width or empty bar.
URLS: https://www.joshwcomeau.com/animation/scroll-driven-animations/, https://developer.mozilla.org/en-US/docs/Web/CSS/Guides/Scroll-driven_animations

### Sliding nav indicator (shared underline)  [medium · day]
WHAT: Instead of each nav link getting its own independent border-bottom on hover, a single thin rose underline lives in the nav bar and glides — with a slight spring overshoot — from whichever link it's under to the one being hovered, snapping back home when the pointer leaves. Both left and right nav groups get their own indicator (they're separate flex groups in the header grid).
WOW: This is the move behind Linear's and Vercel's nav bars — a single continuous object instead of five independent hovers reads as one coherent system, which is exactly the 45-second first-impression bet Katie's site is making on office managers.
FIT: Understated: one rose (accent-only, per palette) line, no color shift, no scale — pure positional motion. Doesn't touch the CTA button, which stays the one loud element.
IMPL: In Header.tsx, wrap NAV_LEFT and NAV_RIGHT each in a relatively-positioned <nav>, add a single absolutely-positioned <span className="nav-indicator"> per group. On mousover of each link (or focus), read link.offsetLeft/offsetWidth relative to the nav container and set indicator style.transform = translateX() / width via a small useRef-based handler (~30 lines, no dependency). Transition with cubic-bezier(0.34,1.56,0.64,1) (a mild spring overshoot) over 250ms. On mouseleave-of-whole-nav, animate back to the active route's link position (compare href to usePathname()).
RISK: Needs correct behavior on resize (recompute on window resize/ResizeObserver) and must not run at all on mobile (nav is hidden lg:flex already, so it's naturally desktop-only) — biggest risk is a stale rect after a font-load layout shift causing an indicator that's slightly misaligned on first paint.
URLS: https://linear.app

### FAQ accordion delight pass  [medium · hours]
WHAT: Opening a question does three small things instead of just the grid-row expand it does now: a 2px rose accent bar grows top-to-bottom along the card's left edge, the plus-to-X icon gets a soft overshoot on rotate instead of a linear turn, and the answer paragraph fades/rises in ~60ms behind the panel's own reveal instead of appearing instantly once the row has height.
WOW: FAQ accordions are usually the most neglected element on a site, so a beautifully choreographed one is disproportionately memorable — it's the exact kind of 'somebody cared about every detail' signal Awwwards-tier sites use accordions to prove.
FIT: Still fundamentally the same disclosure pattern (a11y unchanged — aria-expanded/region already correct), just tuned timing and one restrained rose accent. No new colors, no bounce that reads as playful-app rather than clinical.
IMPL: In FaqAccordion.tsx: add a ::before-style absolutely positioned 2px bar on the card, transform: scaleY(0)→scaleY(1) transform-origin:top, transitioning on the existing `open` state class (150ms delay after the row starts opening so it trails slightly). Change the icon's rotate transition timing-function from the current default to var(--ease-soft) already defined, but bump duration to 350ms for a touch of overshoot feel via a cubic-bezier(0.34,1.56,0.64,1) swap on that one property. For the paragraph, add an inner span with `.reveal`-style opacity/translateY(6px) transition gated on `open`, delayed ~80ms via transition-delay so it visibly trails the panel's height animation.
RISK: Layering three separately-timed transitions on one interaction can look busy if not tuned carefully — needs an actual before/after look, and the grid-template-rows technique already in place makes it easy for the inner delayed transition to get clipped by overflow:hidden before it finishes; test at 350ms total budget.

### Service tile duotone-to-color hover wash  [medium · hours]
WHAT: Service tile photos sit at a very slightly desaturated/warm-toned rest state (a subtle brown-tinted filter, not full grayscale) and bloom into full natural color exactly on hover/focus, synced with the existing 1.045 scale and card lift.
WOW: It's a well-established editorial-magazine motion trick (muted-to-color on interaction) that reads as expensive and intentional rather than default Bootstrap-card hover, and it ties the interaction directly to the brand's own brown ink instead of a generic darken overlay.
FIT: The tint uses the palette's own brown (sepia(.15) contrast(.95) saturate(.85) via CSS filter tuned to sit near --color-brown), so it's warm and on-brand at rest, not gray/cold. The 'coming alive' motion is subtle color, not the site trying to be flashy.
IMPL: In ServiceTiles.tsx, add `filter: sepia(0.12) saturate(0.85) brightness(0.98);` to the base Image className and `group-hover:filter-none group-hover:[filter:none]` (or a matching Tailwind arbitrary value) on hover/focus-within, with `transition-[filter] duration-700 ease-[var(--ease-soft)]` alongside the scale transition already there so both resolve together.
RISK: CSS filter on a full-bleed 250px-tall image forces GPU compositing on every card at once on mobile if scroll triggers repaint — keep transition on filter only (cheap) and confirm on a mid-tier Android that three simultaneous filtered images don't jank; since it's hover-only (not scroll-linked) this should be low risk in practice.

### Restrained magnetic pull on primary CTA buttons  [medium · hours]
WHAT: On desktop pointer devices only, hovering near the hero's 'Contact Katie' button and the brown CTA-band button causes the button to drift a few pixels toward the cursor within a small capture radius, springing back the instant the pointer leaves — never more than about 6px of travel.
WOW: Magnetic buttons are the single most-copied 2025-2026 micro-interaction on premium marketing sites; done at this tiny amplitude it reads as 'this button wants to be clicked' rather than a toy, which is the difference between a corporate-practice CTA and one that actually converts attention into a click.
FIT: Amplitude is the whole game here — capped at ~6px (versus the 20-30px seen on agency/portfolio sites) keeps it dignified. Scoped only to the two highest-intent buttons (not every .btn instance, and never the nav CTA, which stays put so the header remains calm).
IMPL: A small client component MagneticButton.tsx wraps the two Link/btn-orange instances used in Hero.tsx and CtaBand.tsx. On mousemove within a 60px-padded bounding box, compute offsetX/offsetY as (cursorPos - center) * 0.15 capped at ±6px, apply via style.transform = translate3d(x,y,0) using a rAF-throttled handler; on mouseleave, transition back to translate3d(0,0,0) over 300ms with var(--ease-soft). Gate the whole listener behind `window.matchMedia('(hover: hover) and (pointer: fine)')` so touch never attaches listeners at all, and behind prefers-reduced-motion.
RISK: If the capture radius or damping factor is even slightly too strong it stops feeling premium and starts feeling like the button is evading the cursor — needs hands-on tuning, and must be verified it doesn't shift the click hitbox enough to miss taps near the edge (mitigated by touch devices being fully excluded).
URLS: https://en.inithtml.com/resources/magnetic-hover-effect-creating-cursor-attracted-buttons-with-vanilla-javascript/

### Scroll-linked curve breathing + steps connector line  [medium · day]
WHAT: The brown/orange curved-band cutouts (StatementBands, CtaBand) very subtly deepen their curve radius as they cross into view while scrolling, and on the Steps section a hairline path draws itself left-to-right beneath the three numbered circles as the section scrolls into view — visually 'connecting' step 1→2→3 as a journey.
WOW: It converts two currently-static structural devices (the curve, the numbered list) into scroll-choreographed moments without adding a single scroll-event listener — both run purely on the compositor via CSS animation-timeline: view(), which is the actual 2025-2026 replacement for GSAP ScrollTrigger on marketing sites.
FIT: Motion is structural, not decorative — it reinforces the 'three simple steps' narrative and the site's existing curved-band rhythm rather than introducing a new visual idiom. Extremely subtle: a few degrees of curve change and one line draw, each finishing well before the section is half-scrolled past.
IMPL: For the bands: give the arc <span> an `animation: deepen-curve linear both; animation-timeline: view(); animation-range: entry;` keyframe that nudges border-radius/height slightly as it enters. For Steps.tsx: add an absolutely-positioned SVG line spanning the three step numbers with stroke-dasharray/stroke-dashoffset set to path length, then `animation: draw-line linear; animation-timeline: view(); animation-range: contain 0% contain 60%;` driving dashoffset to 0. Both wrapped in `@supports (animation-timeline: view())` with the current static appearance as the unconditional fallback (already true today), plus prefers-reduced-motion: no-preference gate.
RISK: Firefox stable (no scroll-driven-animation support yet) simply never sees the effect — acceptable degrade — but Safari/Chrome behavior differences in animation-range interpretation need a real device pass, not just desktop Chrome DevTools, before calling it done.
URLS: https://developer.chrome.com/blog/scroll-triggered-animations, https://developer.mozilla.org/en-US/docs/Web/CSS/Guides/Scroll-driven_animations/Timelines

### Native View Transitions between pages, with shared header + morphing service photo  [ambitious · days]
WHAT: Navigating anywhere on the site — tile to service page, nav link to nav link — crossfades the outgoing content out and the incoming content in over ~250-400ms instead of a hard reload-flash, the header logo and top bar stay perfectly still throughout (visually 'anchoring' the frame), and clicking a service tile on the homepage morphs that exact photo, in place, into the service page's hero image rather than swapping to a new unrelated image.
WOW: This is THE 2025-2026 platform-native technique — no library, GPU-accelerated, and it's the single interaction that makes a site feel like one continuous, considered space instead of a stack of separate pages, which is exactly the trust signal a skimming office manager registers subconsciously in the first few clicks.
FIT: It's restrained by design: a plain crossfade + very slight upward drift (10px), no bounce, no color flash, respects prefers-reduced-motion by collapsing to an instant swap (the platform default). The photo morph only fires on the one genuinely connected pair (tile → detail hero), so it reads as intentional continuity, not a gimmick applied everywhere.
IMPL: Set `experimental.viewTransition: true` in next.config.ts (already on Next 16.2.6 / React 19.2.4, no canary install needed per Next's own guide). Wrap page content in React's <ViewTransition> (import from 'react'), give the header a stable `viewTransitionName` with `::view-transition-old(site-header){display:none} ::view-transition-group(site-header){animation:none}` so it never re-renders during nav. Give each ServiceTiles image and the matching ServiceHero image a matching `<ViewTransition name={`service-${slug}`}>` wrapper so React/the browser morphs size+position automatically; customize with `share="morph"` and a light 3px blur keyframe mid-flight per Next's documented pattern to hide interpolation artifacts. Add the reduced-motion override zeroing all view-transition animation-durations.
RISK: Experimental flag — Safari view-transition behavior differs from Chrome/Edge (per Next's own docs) and needs a real Safari pass; also requires care that the shared-element name doesn't collide across the 3 service tiles/pages and that Suspense boundaries used elsewhere on the site don't fight the transition. Falls back to a normal instant navigation with zero animation if unsupported, so it's non-destructive to ship.
URLS: https://nextjs.org/docs/app/guides/view-transitions, https://react-view-transitions-demo.labs.vercel.dev

## v2:68c8ad3a609035d70544f04f82c00ada4a9a61b7c1c7aa876eab6cd866655c17  (7 ideas)

### One-time hero line-draw of the monarch mark (the signature moment)  [quick-win · hours]
WHAT: On first paint of the homepage hero, the existing ButterflyMark line-art (already sitting in the header/credential area) draws itself stroke-by-stroke — spine first, then each wing pair — over about 1.3s, then comes to rest. It never repeats: not on scroll re-entry, not on hover, not in a loop.
WOW: It's the one deliberate 'someone crafted this' beat on the site, delivered in the same three seconds a physician's-office manager spends deciding whether to trust the practice — felt in peripheral vision, not demanded. Judgment call on wow vs. kitsch: this reads as WOW specifically because it is small (~28px, the size already used for the mark), monochrome brown, plays exactly once per session, and never loops or 'flaps.' The same animation at hero-photo scale, looping, or in orange/rose would read as a mascot and tip into KITSCH — the boundary is size + frequency, not the technique itself.
FIT: Reuses the mark Katie already approved conceptually rather than inventing a new decoration; zero new facts or copy; costs nothing at the 45-second-decision moment because it doesn't block or delay any content — text and CTA render immediately, the mark animates independently in the corner.
IMPL: In src/components/ui/ButterflyMark.tsx, add `pathLength="1"` to each `<path>`/`<line>` (avoids a getTotalLength() JS call) and a CSS class per path. In globals.css add `@keyframes draw-in { from { stroke-dashoffset: 1; } to { stroke-dashoffset: 0; } }` with `stroke-dasharray: 1; stroke-dashoffset: 1;` at rest, staggered `animation-delay` per path (0ms/150ms/150ms/300ms/300ms), 1.3s `var(--ease-soft)`, `animation-fill-mode: forwards`, wrapped in `@media (prefers-reduced-motion: no-preference)`. Pure CSS, no JS, no IntersectionObserver — it's above the fold so it just runs on load like the existing hero reveal.
RISK: If a future editor bumps the size, adds a loop, or re-triggers it on scroll, it crosses into mascot territory — worth a one-line CSS comment warning against that.
URLS: https://css-tricks.com/svg-line-animation-works/, https://svgenius.design/blog/how-to-animate-svg-stroke-dasharray-for-line-drawings

### Migration-line scroll progress (desktop gutter accent)  [medium · hours]
WHAT: A hairline (1.5px) vertical path in the left gutter of the homepage, olive-colored, that fills in as the visitor scrolls from hero to footer — a quiet nod to the monarch's migration route rather than a literal progress bar.
WOW: It's wayfinding dressed as brand metaphor: a long scroller (an adult patient reading through FAQs) gets an ambient sense of 'how much is left' without a SaaS-style top progress bar, which would read too tech/corporate for this palette.
FIT: Desktop-only, aria-hidden, never competes with CTA orange (uses olive, an ornament-only token per the palette rules); invisible on mobile where screen space is precious; degrades to simply not existing on unsupported browsers rather than showing a broken half-state.
IMPL: Absolute-positioned SVG line spanning a full-height wrapper around the homepage content; `animation-timeline: scroll(root); animation-range: 0% 100%;` driving `stroke-dashoffset` from full length to 0. Gate with `@supports (animation-timeline: scroll())` — declare the line `display: none` by default and only show it inside the @supports block, so unsupported browsers never see a static leftover line. Scope to `@media (min-width: 1024px) and (pointer: fine)`. As of this research, animation-timeline has ~84% global support and Safari added it at v26 (2026), so this is a safe progressive enhancement, not a dependency.
RISK: Scroll-timeline math against `scroll(root)` can behave differently across browsers with sticky headers in the layout; needs a manual scroll-through QA pass in Chrome, Safari 26+, and Firefox before shipping, and must be excluded entirely under prefers-reduced-motion.
URLS: https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/animation-timeline, https://caniuse.com/mdn-css_properties_animation-timeline_view, https://www.joshwcomeau.com/animation/scroll-driven-animations/

### Statement-band curve draws itself in on scroll (view()-scrubbed)  [medium · hours]
WHAT: The existing curved white lip between the hero and the brown statement band (the `border-radius: 0 0 60%/100%` shape in StatementBands.tsx) gets a companion 2px stroked outline that traces itself along the curve, scrubbed directly to the user's own scroll speed rather than firing once like the current IntersectionObserver reveals.
WOW: It turns a static geometric transition into something that feels hand-drawn exactly once, tied to the wing-curve shape the band already uses — reinforcing the butterfly motif through form, not through adding a literal butterfly icon.
FIT: Purely additive to a component that already exists; low-contrast (thin stroke, brown-on-white) so it reads as texture/craft, not as a new decorative element competing with the orange CTA or the copy.
IMPL: Add a thin stroked path following the curve's arc inside the existing white-lip `<span>` (converted to an inline SVG path matching the border-radius curve). `animation-timeline: view(); animation-range: entry 0% cover 40%;` animates `stroke-dashoffset` from full-length to 0 as the band scrolls into view. Set the resting/no-JS state to fully-drawn (dashoffset: 0) so `@supports not (animation-timeline: view())` browsers simply see the finished line — never a half-drawn broken state.
RISK: Requires converting a CSS border-radius trick into an actual SVG path that matches it pixel-for-pixel across breakpoints — the fiddliest part of this idea; scope to desktop/tablet only if the math doesn't hold up at narrow widths.
URLS: https://css-tricks.com/creating-scroll-based-animations-in-full-view/, https://design.dev/guides/scroll-timeline/

### Tab-away favicon swap (closed-wing state)  [quick-win · hours]
WHAT: When a visitor switches away from the tab, the favicon quietly swaps from the open-wing monarch mark to a closed-wing silhouette (profile view, same line weight) and swaps back the instant they return. No title-bar text tricks, no counters — just the visual.
WOW: The kind of detail only a returning, attentive visitor (a patient re-checking the tab, an office manager keeping it open while comparing referral options) would ever notice — and when they do, it reads as craft rather than gimmick because it's silent.
FIT: Deliberately skips the 'Come back!' title-bar copy pattern that Gmail/Slack use (too aggressive/attention-grabbing for a clinical brand) — pure visual-only, reversible, no data collection, nothing that touches the CSP-restricted areas.
IMPL: Small client component in `layout.tsx` listening for `visibilitychange`; on hidden, swap `<link rel="icon">` href to a second precomputed data-URI/SVG built from the same ButterflyMark path data redrawn wings-closed; on visible, restore the original favicon href. ~1KB, no canvas needed since it's a static second SVG, not an animated one.
RISK: Some browsers cache/ignore dynamic favicon swaps inconsistently (notably older Safari) — verify in the three major engines; low stakes since failure just means the favicon doesn't change, nothing breaks.
URLS: https://www.hongkiat.com/blog/dont-go-favicon-title/, https://css-tricks.com/the-making-of-an-animated-favicon/

### Whisper-grain paper texture on the cream ground  [quick-win · hours]
WHAT: An almost-invisible (2–4% opacity) fine grain texture laid over the cream background site-wide, so the page reads as warm paper stock rather than a flat digital hex color.
WOW: Directly answers Katie's brief in material terms — 2025–2026 'warm minimalism' design writing repeatedly points to visible grain/tactile texture as the difference between 'flat pastel' and 'quiet luxury.' It's a texture change, not a visual element, so nothing competes with photos or CTAs.
FIT: Zero new facts, zero new color, purely a texture pass on the approved cream token; stays well under the CSS/JS performance budget.
IMPL: Pre-render a small (~150–200px) tiling grain PNG offline using an SVG `feTurbulence`/`feColorMatrix` filter (fractalNoise, low baseFrequency) rather than computing the filter live in-browser — freeCodeCamp's writeup flags that live SVG-filter noise over large areas is a real paint-performance cost, especially on mobile, so bake it to a static ~3–5KB tile instead. Apply as `background-image` on a fixed `body::before` layer, `background-repeat: repeat`, `mix-blend-mode: multiply`, opacity 0.03. Verify Lighthouse LCP/CLS unaffected and that AA text contrast still holds with the layer present.
RISK: Must bake to a static tile (not live feTurbulence) to stay inside the performance budget; re-check contrast ratios on the FAQ/form text after adding the layer.
URLS: https://www.freecodecamp.org/news/grainy-css-backgrounds-using-svg-filters/, https://css-tricks.com/grainy-gradients/, https://www.interluxinteriors.com/the-rise-of-warm-minimalism-a-new-year-a-new-sensory-language-of-luxury-for-early-2026

### Reserved cursor accent ring (desktop, fine-pointer only)  [quick-win · hours]
WHAT: A small hollow rose-tinted ring rides alongside (not replacing) the native cursor on desktop, growing slightly and shifting to orange when hovering a button or link, and simply not present on touch devices or near the contact form.
WOW: 2025 design writing on custom cursors is explicit that a thin ring/dot — one accent color, no glow, no trailing physics — is the version that reads as 'calm brand,' as opposed to the wild-shape trend that reads as agency-portfolio.
FIT: Reuses the rose token exactly as scoped in the approved palette ('accents only, never text on cream') rather than adding new color; explicitly excluded from the contact form and its inputs so it never interferes with the one HIPAA-sensitive interaction on the site; native cursor is never hidden, so no accessibility regression.
IMPL: Single fixed-position 16–20px div following `mousemove` via `requestAnimationFrame` at 1:1 speed (no lag/inertia math — keeps it feeling precise, not effect-y); CSS `transition` only on `transform: scale()` and `background/border-color` (150ms); gated by `@media (pointer: fine)`; disabled entirely under `prefers-reduced-motion`; excluded via a `:not(.contact-form *)` scope or by simply not mounting the listener inside the ContactForm subtree.
RISK: Easy to overbuild into a trailing-blob effect if scope creeps — keep the implementation to transform+color only, no blur, no shadow, no multi-frame lag, per the research's own 'one effect is enough' guidance.
URLS: https://www.thinslicedigital.com/custom-cursors/, https://designbeep.com/2025/06/13/20-pure-css-and-javascript-custom-cursor-effects-with-code/

### Custom 404: 'This page has migrated' — the one page allowed to be playful  [medium · hours]
WHAT: On the 404 page only, the ButterflyMark draws itself in (the idea-1 technique), holds for a beat, then gently drifts and fades off the top-right corner of the viewport over ~2.5s — one monarch departing — alongside copy like 'This page has migrated. Here's where the flock actually lives:' and links back to Home, Services, and Contact.
WOW: This is where the site gets to fully use the butterfly-flight motif without the wow-vs-kitsch tension that would exist on the homepage — a 404 is explicitly licensed for a small joke, so the fuller expression of the technique (line-draw + exit flight) lives here instead of on any revenue page.
FIT: Doesn't touch the homepage or any service page audience #1 or #2 ever evaluates in their 45-second window; copy stays dry/clinical ('migrated,' not 'oops! this butterfly flew away!'); zero new facts, no new assets beyond the existing mark.
IMPL: Extend `src/app/not-found.tsx`: reuse the idea-1 draw-in keyframes, then chain a second `@keyframes fly-off { to { transform: translate(40px, -60px) rotate(8deg); opacity: 0; } }` via `animation-delay`. Single `<ButterflyMark>` instance, no new SVG asset. Under `prefers-reduced-motion`, render the mark already at rest with the copy shown immediately (no motion, no delay).
RISK: Keep the copy to one restrained line — a whole paragraph of butterfly puns would undercut the 'clinician talking to a colleague' tone even on a utility page.
URLS: https://css-tricks.com/svg-line-animation-works/
